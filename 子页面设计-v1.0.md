# Skill Dashboard — 子页面设计文档 v1.0

> **版本**: v1.0  
> **日期**: 2026-04-13  
> **依赖**: 数据契约文档-v1.0.md · UI设计文档-v1.0.md  
> **状态**: 设计完成，待实现

---

## 目录

1. [整体导航架构调整](#1-整体导航架构调整)  
2. [技能图谱 Skill Map](#2-技能图谱-skill-map)  
3. [安装市场 Marketplace](#3-安装市场-marketplace)  
4. [健康检查 Health Check](#4-健康检查-health-check)  
5. [新增数据类型](#5-新增数据类型)  
6. [实现顺序建议](#6-实现顺序建议)

---

## 1. 整体导航架构调整

### 1.1 现状 → 改动

```
当前侧边栏（分类过滤器）          新侧边栏（功能页导航）
─────────────────────────       ─────────────────────────────
分类                             主页面
  所有技能         21              ◉ 技能库          所有 Skills
  编程开发          3
  内容创作          4              工具
  数据分析          4              ○ 技能图谱        Skill Map
  产品设计          3              ○ 安装市场        Marketplace
  效率流程          7              ○ 健康检查        Health Check
  商业营销          0
  其他              0
                                  ────────────────────
                                  重新扫描  ↺
```

**关键设计决策**：  
- 分类过滤器下沉到「技能库」页面顶部的 Filter Chips（不消失，只是移位）  
- 侧边栏从「过滤器」升级为「页面路由」  
- 激活状态保持原有的 `border-l-2 border-accent` 样式，继承 UI 设计文档

### 1.2 路由方案

```typescript
// 无需引入 React Router，用 state 内部路由即可（单页轻量）
type AppPage = 'skills' | 'map' | 'marketplace' | 'health';
```

侧边栏传 `currentPage` 和 `onNavigate` prop，`App.tsx` 条件渲染对应页面组件。

---

## 2. 技能图谱 Skill Map

### 2.1 核心定位

> 「让你看清自己的 Skill 生态——哪里稠密、哪里空白、哪两个 Skill 其实在做一件事。」

这不是装饰性图表，是一个**诊断工具**：帮助用户发现 Skill 重叠、能力盲区、分类不合理的地方。

### 2.2 可视化方案选型

| 方案 | 效果 | 实现成本 | 推荐度 |
|------|------|---------|--------|
| Force-directed 力导向图（D3） | 最强，节点自由分布 | 高（需引入 D3） | ⭐⭐⭐ 进阶 |
| **气泡分组图 Bubble Cluster** | 强，按分类聚类成圆形群组 | 中（纯 CSS + SVG） | ⭐⭐⭐⭐⭐ 推荐 |
| Treemap 矩形树图 | 清晰，面积代表数量 | 低 | ⭐⭐⭐ 备选 |
| 固定网格布局 | 无动效，平铺 | 最低 | ⭐⭐ 不推荐 |

**采用方案：气泡分组图（Bubble Cluster）**  
- 每个分类是一个大圆形「星团」，内含该分类下的 Skill 小气泡  
- 气泡大小 = tags 数量（代表覆盖广度）  
- 气泡颜色 = 分类色（与卡片标签色系一致）  
- 小气泡之间绘制连线 = 共享 2 个以上 tag 的 Skill（关联关系）

### 2.3 页面布局

```
┌─────────────────────────────────────────────────────────────┐
│  顶部控制栏                                                   │
│  [分组方式: 分类▼]  [关联阈值: ≥2个Tag▼]  [显示标签 ●]       │
│  ───────────────────────────────────────────────────────── │
│                                                             │
│                   [  SVG 图谱区域  ]                         │
│                                                             │
│    ┌──────────┐          ┌──────────┐                       │
│    │  编程开发  │          │  效率流程  │                      │
│    │  ●●●     │    ╌╌╌   │  ●●●●●●● │                      │
│    └──────────┘          └──────────┘                       │
│                                                             │
│       ┌──────────┐    ┌──────────┐                          │
│       │  内容创作  │    │  数据分析  │                        │
│       │  ●●●●    │    │  ●●●●    │                         │
│       └──────────┘    └──────────┘                          │
│                                                             │
│─────────────────────────────────────────────────────────── │
│  详情栏（hover 触发，固定在底部）                               │
│  [Playwright 自动化]  编程开发 · 3 个关联 Skill               │
└─────────────────────────────────────────────────────────────┘
```

### 2.4 交互规格

| 操作 | 反馈 |
|------|------|
| hover 小气泡 | 气泡放大 1.2x + 底部详情栏显示 Skill 名和关联列表 |
| click 小气泡 | 跳转回技能库页面并打开该 Skill 的右侧面板 |
| hover 连线 | 连线高亮为 `--accent-primary`，两端气泡同时高亮 |
| 拖拽分类大圆 | 重新排列布局（可选，进阶功能） |
| 分组方式切换 | 平滑过渡动画，气泡重新聚类 |

### 2.5 连线计算逻辑

```typescript
// 两个 Skill 之间的关联分数
function calcRelation(a: Skill, b: Skill): number {
  const sharedTags = a.tags.filter(t => b.tags.includes(t)).length;
  const sameTrigger = a.details.triggerWords.some(tw =>
    b.details.triggerWords.includes(tw)
  ) ? 1 : 0;
  return sharedTags + sameTrigger;
}

// 阈值 ≥ 2 才绘制连线
const edges = skills.flatMap((a, i) =>
  skills.slice(i + 1).map(b => ({ a, b, score: calcRelation(a, b) }))
).filter(e => e.score >= threshold);
```

### 2.6 空状态 & 边缘情况

- Skills < 3 个：显示「技能太少，图谱需要至少 3 个 Skill 才有意义」  
- 无连线（所有 Skill 完全不同）：提示「未发现关联 Skill，尝试降低关联阈值」  
- 单分类：不显示聚类大圆，直接平铺气泡

---

## 3. 安装市场 Marketplace

### 3.1 核心定位

> 「你的 Skill 仓库不应该只有你手动写的那些——这里有别人整理好的，一键装进来。」

数据来源：`openai/skills` GitHub 仓库的策划列表（Mock 阶段写死，真实接入时调 GitHub API 或代理接口）。

### 3.2 页面布局

```
┌──────────────────────────────────────────────────────────────┐
│  Marketplace · 发现新能力                                      │
│  从 openai/skills 官方策划库探索，一键添加到你的技能库           │
│                                                              │
│  [🔍 搜索可安装的 Skill...]           [已安装: 21]  [可用: 47] │
│                                                              │
│  [全部] [编程开发] [内容创作] [数据分析] ...  Filter Chips       │
│  ─────────────────────────────────────────────────────────  │
│                                                              │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐         │
│  │ git-ops      │ │ code-review  │ │ web-search   │         │
│  │ Git 操作助手  │ │ 代码审查专家  │ │ 联网搜索      │         │
│  │ ···          │ │ ···          │ │ ···          │         │
│  │ [✓ 已安装]   │ │ [+ 安装]     │ │ [+ 安装]     │         │
│  └──────────────┘ └──────────────┘ └──────────────┘         │
│                                                              │
│  共 47 个可用 Skill，已安装 21 个                              │
└──────────────────────────────────────────────────────────────┘
```

### 3.3 卡片设计规格

```
┌────────────────────────────────────┐
│  🌐  web-search          编程开发   │  ← icon + name + category badge
│  ────────────────────────────────  │
│  让 Codex 通过 Brave Search 联网    │  ← description (2行截断)
│  搜索实时信息，附引用链接。            │
│  ────────────────────────────────  │
│  #search  #web  #realtime          │  ← tags
│                                    │
│  ⭐ 官方策划  · 2.3k installs       │  ← meta（mock 数据）
│                              [安装] │  ← CTA button
└────────────────────────────────────┘
```

**状态区分**：
- `已安装`：按钮变为 `✓ 已安装`，灰色禁用，卡片右上角显示 `已拥有` badge  
- `安装中`：按钮显示 loading spinner  
- `可安装`：按钮为 `+ 安装`，`--accent-primary` 色

### 3.4 安装交互流（Mock → 真实）

```
用户点击「安装」
    │
    ▼
Mock 阶段：
  - 按钮变 loading (800ms)
  - 模拟安装成功
  - 显示 toast: "git-ops 已添加到你的技能库"
  - 该卡片状态变为「已安装」
  - 技能库页面的 Skills 列表追加该条目

真实接入阶段：
  - 调用 POST /api/marketplace/install
  - 后端执行 git clone / 文件复制 到 ~/.codex/skills/
  - 触发扫描，返回新 Skill 信息
```

### 3.5 Mock 市场数据设计（精选 15 条）

```typescript
export interface MarketplaceItem {
  id: string;
  name: string;
  description: string;
  category: Category;
  tags: string[];
  author: 'official' | 'community';  // official = openai/skills 策划
  installs: number;                  // mock 安装量
  githubPath: string;                // 真实 GitHub 路径
  isInstalled: boolean;              // 是否已在本地
}
```

**15 条 Mock 数据覆盖方向**：

| id | 名称 | 分类 | 已覆盖 |
|----|------|------|--------|
| `git-ops` | Git 操作助手 | 编程开发 | ✗ |
| `code-review` | 代码审查 | 编程开发 | ✗ |
| `web-search` | 联网搜索 | 数据分析 | ✗ |
| `sql-analyst` | SQL 数据分析 | 数据分析 | ✗ |
| `copywriting` | 营销文案 | 商业营销 | ✗ |
| `social-media` | 社媒内容 | 商业营销 | ✗ |
| `email-writer` | 邮件写作 | 内容创作 | ✗ |
| `video-script` | 视频脚本 | 内容创作 | ✗ |
| `ux-research` | UX 研究 | 产品设计 | ✗ |
| `competitive-analysis` | 竞品分析 | 产品设计 | ✗ |
| `meeting-notes` | 会议纪要 | 效率流程 | ✗ |
| `docker-ops` | Docker 运维 | 编程开发 | ✗ |
| `data-viz` | 数据可视化 | 数据分析 | ✗ |
| `content-calendar` | 内容日历 | 商业营销 | ✗ |
| `api-docs` | API 文档生成 | 编程开发 | ✗ |
| *(用户已有的 21 个)* | — | — | ✓ |

这样「商业营销」和「其他」两个 0-count 分类就有数据了，侧边栏不再显示空分类。

### 3.6 空状态

- 搜索无结果：「没有找到 "xxx" 相关的 Skill，试试其他关键词」  
- 全部已安装：「🎉 市场里的 Skill 你都有了，继续关注更新」

---

## 4. 健康检查 Health Check

### 4.1 核心定位

> 「你的 Skill 库不只要「有」，还要「好用」。完整度评分告诉你哪些 Skill 只是个空壳。」

这是最贴近维护工作流的功能：扫描所有 Skill，给出量化评分，点击直接跳转编辑。

### 4.2 评分模型

#### 完整度评分（Completeness Score，0-100 分）

```typescript
interface CompletenessCheck {
  field: string;
  label: string;
  points: number;
  required: boolean;
  validator: (skill: Skill) => boolean;
}

const CHECKS: CompletenessCheck[] = [
  {
    field: 'title',
    label: '标题不为空',
    points: 15,
    required: true,
    validator: s => s.title.trim().length > 0,
  },
  {
    field: 'description',
    label: '描述 ≥ 20 字',
    points: 20,
    required: true,
    validator: s => s.description.trim().length >= 20,
  },
  {
    field: 'category',
    label: '分类已指定（非「其他」）',
    points: 10,
    required: false,
    validator: s => s.category !== '其他',
  },
  {
    field: 'tags',
    label: '至少 2 个标签',
    points: 10,
    required: false,
    validator: s => s.tags.length >= 2,
  },
  {
    field: 'whenToUse',
    label: '至少 2 条使用场景',
    points: 15,
    required: false,
    validator: s => s.details.whenToUse.length >= 2,
  },
  {
    field: 'whatItDoes',
    label: '详细描述 ≥ 50 字',
    points: 20,
    required: false,
    validator: s => s.details.whatItDoes.trim().length >= 50,
  },
  {
    field: 'rawContent',
    label: '原始内容已填写',
    points: 10,
    required: false,
    validator: s => s.details.rawContent.trim().length >= 50,
  },
];

function calcScore(skill: Skill): number {
  return CHECKS.reduce((sum, check) =>
    sum + (check.validator(skill) ? check.points : 0), 0
  );
}
```

#### 等级划分

| 分数 | 等级 | 颜色 | 含义 |
|------|------|------|------|
| 90-100 | 优秀 ✦ | `--success` 绿 | 完整，随时可用 |
| 70-89 | 良好 | `--accent-primary` 青 | 可用，有小瑕疵 |
| 50-69 | 待完善 | `--warning` 橙 | 能用但不够清晰 |
| 0-49 | 需修复 | `--danger` 红 | 缺失关键信息 |

### 4.3 页面布局

```
┌──────────────────────────────────────────────────────────────┐
│  Health Check · 技能质量检查                                   │
│                                                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │  21      │ │  18      │ │   3      │ │   0      │        │
│  │ 总技能数  │ │ 健康      │ │ 需完善    │ │ 需修复    │        │
│  │          │ │ 86%      │ │ 14%      │ │  0%      │        │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │
│                                                              │
│  整体完整度  ████████████████████░░░░  86 / 100              │
│                                                              │
│  [全部] [需修复 ●] [待完善 ●] [良好] [优秀]  Filter           │
│  [字段: 全部▼]  [排序: 分数低→高▼]                            │
│  ─────────────────────────────────────────────────────────  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐    │
│  │  ● analyst            ████████████████░░░░  82分 良好 │    │
│  │    ✓ 标题  ✓ 描述  ✓ 分类  ✓ 标签  ✗ 使用场景 ✓ 详情   │    │
│  │                                       [去完善 →]      │    │
│  ├──────────────────────────────────────────────────────┤    │
│  │  ● jupyter-notebook   ████████░░░░░░░░░░░  55分 待完善│    │
│  │    ✓ 标题  ✓ 描述  ✗ 分类  ✗ 标签  ✗ 使用场景 ✓ 详情   │    │
│  │                                       [去完善 →]      │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                              │
│  详情展开区（点击行展开）                                       │
│  ┌─────────────────────────────────────────────────────┐     │
│  │  缺失字段说明：                                        │     │
│  │  ✗ 使用场景 — 建议填写 2-4 条具体触发场景               │     │
│  │  💡 示例：「当需要分析表格数据时」「生成 EDA 报告时」     │     │
│  └─────────────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────────┘
```

### 4.4 列表行设计

每行 Skill 包含三个区域：

```
[状态圆点] [Skill 名称] [进度条 + 分数 + 等级 badge]  [操作]
           [字段检查图标行: ✓✓✓✓✗✓✓]                [去完善→]
```

**字段图标行**：
- ✓ 绿色：该字段通过检查  
- ✗ 红色：该字段未通过，悬停显示 tooltip 说明原因  
- 图标顺序：标题 · 描述 · 分类 · 标签 · 使用场景 · 详情 · 原始内容

**「去完善」按钮**：
- 点击后跳转至「技能库」页面，并自动展开该 Skill 的右侧面板，且**直接进入编辑模式**

```typescript
// 跳转逻辑
onNavigate('skills');
onSelectSkill(skill.id);
onEnterEditMode(skill.id);  // 触发 RightPanel 自动进入 isEditing = true
```

### 4.5 顶部汇总卡片设计

```typescript
interface HealthSummary {
  totalSkills: number;
  excellent: number;   // 90-100
  good: number;        // 70-89
  needsWork: number;   // 50-69
  critical: number;    // 0-49
  avgScore: number;
  mostCommonIssue: string;  // "缺少使用场景" | "描述过短" | ...
}
```

**最常见问题**区域：
```
⚠ 最常见问题：7 个 Skill 缺少「使用场景」字段
   → [一键查看全部]
```

### 4.6 进度条组件规格

```
完整度进度条：
- 宽度：固定 120px（列表行）/ 200px（展开状态）
- 高度：4px
- 背景：var(--bg-elevated)
- 填充色：根据分数段变色
  < 50: var(--danger)
  < 70: var(--warning)
  < 90: var(--accent-primary)
  ≥ 90: var(--success)
- 动画：页面加载时从 0 扩展到实际宽度（200ms ease-out）
```

### 4.7 字段过滤器

顶部 `[字段: 全部▼]` 下拉菜单可选具体字段：
- 全部（默认）
- 仅显示「缺少使用场景」的 Skill
- 仅显示「描述过短」的 Skill
- 仅显示「分类为其他」的 Skill
- 仅显示「无标签」的 Skill

这样可以批量发现同类问题，效率更高。

---

## 5. 新增数据类型

```typescript
// src/types.ts 追加

// ── Skill Map ──────────────────────────────────

export interface SkillEdge {
  sourceId: string;
  targetId: string;
  score: number;     // 关联分数（共享 tag 数 + trigger 重叠）
  sharedTags: string[];
}

export interface SkillMapData {
  nodes: Skill[];
  edges: SkillEdge[];
}

// ── Marketplace ────────────────────────────────

export type MarketplaceAuthor = 'official' | 'community';

export interface MarketplaceItem {
  id: string;
  name: string;
  description: string;
  category: Category;
  tags: string[];
  author: MarketplaceAuthor;
  installs: number;
  githubPath: string;
  isInstalled: boolean;   // 通过 id 与本地 SKILLS 比对
}

export interface MarketplaceInstallResult {
  success: boolean;
  installedSkill?: Skill;
  error?: string;
}

// ── Health Check ───────────────────────────────

export interface FieldCheckResult {
  field: string;
  label: string;
  passed: boolean;
  points: number;
  tip?: string;    // 未通过时的改进建议
}

export interface SkillHealthReport {
  skillId: string;
  score: number;             // 0-100
  grade: 'excellent' | 'good' | 'needs-work' | 'critical';
  checks: FieldCheckResult[];
  passedCount: number;
  totalChecks: number;
}

export interface HealthSummary {
  totalSkills: number;
  excellent: number;
  good: number;
  needsWork: number;
  critical: number;
  avgScore: number;
  mostCommonIssue: string;
  reports: SkillHealthReport[];
}
```

---

## 6. 实现顺序建议

### Phase 1：导航架构（½天）

1. 修改 `Sidebar.tsx`：从分类过滤器改为页面导航  
2. 修改 `App.tsx`：添加 `currentPage` state，条件渲染页面组件  
3. 将原分类 Filter Chips 移到「技能库」页面顶部

### Phase 2：健康检查（1天）⬅ 优先

> 最实用，逻辑最独立，不依赖外部数据

1. 新建 `src/components/pages/HealthCheck.tsx`  
2. 实现 `calcCompletenessScore()` 工具函数（`src/utils.ts` 追加）  
3. 实现汇总卡片 + 列表行组件  
4. 接入「去完善」跳转逻辑（App 层状态提升）

### Phase 3：安装市场（1天）

> 逻辑完整，Mock 数据驱动，视觉效果明显

1. 新建 `src/constants/marketplace.ts`（15 条 Mock 数据）  
2. 新建 `src/components/pages/Marketplace.tsx`  
3. `mockApi` 追加 `installSkill()` 方法  
4. 实现安装动画和 toast 反馈

### Phase 4：技能图谱（1.5天）

> 视觉复杂度最高，放最后

1. 新建 `src/components/pages/SkillMap.tsx`  
2. 实现 `calcSkillEdges()` 工具函数  
3. 实现 SVG 气泡分组图（纯 SVG，不引入 D3）  
4. 实现 hover 交互和底部详情栏

---

## 附录：组件文件结构建议

```
src/
├── components/
│   ├── layout/
│   │   ├── Sidebar.tsx        ← 改造为页面导航
│   │   └── TopBar.tsx
│   ├── pages/
│   │   ├── SkillLibrary.tsx   ← 当前主页面内容抽离
│   │   ├── SkillMap.tsx       ← 新建
│   │   ├── Marketplace.tsx    ← 新建
│   │   └── HealthCheck.tsx    ← 新建
│   ├── shared/
│   │   ├── SkillCard.tsx
│   │   ├── StatsCard.tsx
│   │   ├── RightPanel.tsx
│   │   ├── ScoreBar.tsx       ← 新建，复用于 HealthCheck
│   │   └── StatusBadge.tsx    ← 抽离现有 badge 逻辑
│   └── map/
│       ├── BubbleCluster.tsx  ← Skill Map 专用
│       └── SkillNode.tsx
├── constants/
│   ├── skills.ts              ← 原 constants.ts 拆分
│   └── marketplace.ts         ← 新建
├── utils/
│   ├── time.ts                ← formatRelativeTime
│   ├── health.ts              ← calcCompletenessScore 等
│   └── map.ts                 ← calcSkillEdges 等
└── mock.ts
```

---

*文档版本 v1.0 · 2026-04-13 · 可直接作为开发输入文档使用*
