# Skill Dashboard — 数据契约文档 v1.0

> **版本**: v1.0
> **日期**: 2026-04-13
> **技术栈**: React 19 + Vite + TypeScript + Express (本地)
> **状态**: 可用于开发

---

## 总览：数据流决策

```
当前状态           MVP Demo          后续真实接入
────────────      ────────────      ────────────────────
constants.ts  →   Mock API层    →   Express + 文件系统扫描
(写死数据)         (前端假请求)       (真实读取 .codex/skills/)
```

**原则**：先跑通 UI 和交互，Mock 数据与真实接口共用同一套 TypeScript 类型，切换成本为零。

---

## 一、数据模型（类型定义）

### 1.1 核心实体：Skill

```typescript
// src/types.ts — 完整版（替换现有文件）

/** Skill 文件来源类型 */
export type SkillFileType = 'md' | 'json' | 'txt';

/** Skill 分类 — 与侧边栏 / Filter Chip 对齐
 *  参考 awesome-agent-skills 生态的真实分类体系（2026）
 *  - 编程开发：code review、架构、frontend、git
 *  - 内容创作：写作、PPT、公众号、推文、图像生成
 *  - 数据分析：research、摘要、CSV、可视化
 *  - 产品设计：UX、PRD、用户研究、原型反馈
 *  - 效率流程：SOP、自动化工作流、checklist
 *  - 商业营销：营销文案、竞品分析、销售话术
 *  - 其他：兜底
 */
export type Category =
  | '编程开发'
  | '内容创作'
  | '数据分析'
  | '产品设计'
  | '效率流程'
  | '商业营销'
  | '其他';

/** Skill 识别状态 */
export type SkillStatus =
  | 'active'       // 已识别，可用 → badge-success
  | 'unrecognized' // 未能解析元信息 → badge-warning
  | 'updating';    // 扫描中 → badge-accent

/** Skill 详情内容（右侧面板展示） */
export interface SkillDetail {
  whatItDoes: string;       // 这个 Skill 能做什么（段落描述）
  whenToUse: string[];      // 适用场景列表（bullet points）
  triggerWords: string[];   // 触发词 / 调用关键词（如 "write email"）
  rawContent: string;       // 原始文件内容（用于代码预览区）
}

/** Skill 主实体 */
export interface Skill {
  id: string;               // 唯一ID，由文件名 slug 化生成（如 "ts-arch"）
  title: string;            // 展示名称（从文件元信息 or 文件名推断）
  description: string;      // 一句话描述（卡片摘要，≤50字）
  category: Category;       // 所属分类
  tags: string[];           // 标签列表（用于 Filter Chip）
  status: SkillStatus;      // 识别状态
  icon: string;             // Lucide 图标名（如 "Code2"）或 emoji
  sourcePath: string;       // 源文件完整路径（如 ~/ai/skills/coding/ts-arch.md）
  fileName: string;         // 文件名（如 ts-arch.md）
  fileType: SkillFileType;  // 文件类型
  updatedAt: string;        // 文件最后修改时间（ISO 8601 或 "3天前"）
  scannedAt: string;        // 最近一次被扫描到的时间（ISO 8601）
  details: SkillDetail;     // 详情数据（右侧面板用）
}

/** 统计数字（Hero Stats 区） */
export interface StatsData {
  totalSkills: number;       // 总 Skill 数
  totalCategories: number;   // 覆盖分类数
  newSinceLastScan: number;  // 上次扫描后新增数
  lastScanTime: string;      // 最近扫描时间（"3分钟前" 或 ISO 8601）
}

/** App 配置（扫描路径等） */
export interface AppConfig {
  scanPaths: string[];       // 要扫描的目录路径列表
  autoScan: boolean;         // 是否启动时自动扫描
  lastScanAt: string | null; // 最后一次扫描时间
}

/** 扫描结果 */
export interface ScanResult {
  success: boolean;
  totalFound: number;        // 本次扫描发现的文件总数
  newCount: number;          // 新增
  updatedCount: number;      // 有变化（内容更新）
  failedCount: number;       // 解析失败
  scannedAt: string;         // 扫描完成时间
  errors: string[];          // 失败文件的错误信息
}

/** API 统一响应包装 */
export interface ApiResponse<T> {
  success: boolean;
  data: T;
  error?: string;
}
```

---

### 1.2 字段说明速查表

| 字段 | 类型 | 来源 | 展示位置 |
|------|------|------|---------|
| `id` | `string` | 文件名 slug | 内部 key |
| `title` | `string` | 文件 frontmatter / 首个 `#` 标题 | 卡片标题、详情标题 |
| `description` | `string` | frontmatter `description` 字段 | 卡片摘要（2行截断） |
| `category` | `Category` | frontmatter `category`（枚举：编程开发/内容创作/数据分析/产品设计/效率流程/商业营销/其他） | 侧边栏计数、Filter Chip |
| `tags` | `string[]` | frontmatter `tags` 数组 | 卡片底部标签、搜索匹配 |
| `status` | `SkillStatus` | 解析成功与否判断 | 卡片右上角状态 Badge |
| `icon` | `string` | frontmatter `icon`，缺省按分类映射 | 卡片图标、详情图标 |
| `sourcePath` | `string` | 文件绝对路径 | 详情面板元数据行 |
| `updatedAt` | `string` | 文件 mtime | 卡片底部时间戳 |
| `details.rawContent` | `string` | 文件全文 | 详情面板代码预览区 |
| `details.whenToUse` | `string[]` | frontmatter `when_to_use` | 详情面板场景 bullets |

---

## 二、API 接口定义

### 2.1 整体路由表

| 方法 | 路径 | 描述 | 数据来源 |
|------|------|------|---------|
| `GET` | `/api/skills` | 获取全部 Skill 列表（支持筛选） | Mock / 文件扫描 |
| `GET` | `/api/skills/:id` | 获取单个 Skill 详情 | Mock / 文件扫描 |
| `PUT` | `/api/skills/:id` | **编辑 Skill 字段，写回原始文件** | Mock / fs.writeFile |
| `GET` | `/api/stats` | 获取 Hero 区统计数据 | Mock / 聚合计算 |
| `POST` | `/api/scan` | 触发一次文件扫描 | 文件系统（真实接口） |
| `GET` | `/api/scan/status` | 获取最近扫描状态 | 内存缓存 |
| `GET` | `/api/config` | 获取 App 配置 | 本地 JSON 文件 |
| `PUT` | `/api/config` | 更新配置（扫描路径等） | 本地 JSON 文件 |

---

### 2.2 详细接口规范

#### `GET /api/skills`

获取 Skill 列表，支持关键词搜索和分类筛选。

**请求参数（Query Params）**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `q` | `string` | 否 | 关键词，匹配 title / description / tags |
| `category` | `Category` | 否 | 按分类筛选，不传返回全部 |
| `status` | `SkillStatus` | 否 | 按状态筛选 |

**返回格式**：

```typescript
// HTTP 200
{
  "success": true,
  "data": {
    "skills": Skill[],
    "total": number
  }
}

// 示例
{
  "success": true,
  "data": {
    "skills": [
      {
        "id": "ts-arch",
        "title": "TypeScript 架构师",
        "description": "系统级架构规划和复杂类型重构代理。",
        "category": "编程",
        "tags": ["typescript", "architecture", "refactor"],
        "status": "active",
        "icon": "Code2",
        "sourcePath": "~/.codex/skills/coding/ts-arch.md",
        "fileName": "ts-arch.md",
        "fileType": "md",
        "updatedAt": "2026-04-10T08:30:00Z",
        "scannedAt": "2026-04-13T10:00:00Z",
        "details": {
          "whatItDoes": "分析复杂的 TypeScript 代码库...",
          "whenToUse": ["重构遗留逻辑", "初始项目脚手架"],
          "triggerWords": ["refactor", "typescript", "架构"],
          "rawContent": "# TypeScript 架构师\n\n..."
        }
      }
    ],
    "total": 23
  }
}
```

---

#### `GET /api/skills/:id`

获取单个 Skill 的完整详情（包含 rawContent）。

**路径参数**：

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | `string` | Skill 的唯一 ID（如 `ts-arch`） |

**返回格式**：

```typescript
// HTTP 200 — 找到
{
  "success": true,
  "data": Skill  // 同列表格式，rawContent 保证完整
}

// HTTP 404 — 不存在
{
  "success": false,
  "error": "Skill not found: ts-arch"
}
```

---

#### `GET /api/stats`

获取 Hero 区统计聚合数据。

**返回格式**：

```typescript
// HTTP 200
{
  "success": true,
  "data": {
    "totalSkills": 23,
    "totalCategories": 5,
    "newSinceLastScan": 4,
    "lastScanTime": "3分钟前"   // 前端展示用的相对时间字符串
  }
}
```

---

#### `PUT /api/skills/:id`

**编辑 Skill 字段，并将修改写回原始 `.md` 文件的 frontmatter。**

只需传入要修改的字段（Partial），不传的字段保持原值不变。

**路径参数**：

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | `string` | Skill 的唯一 ID（如 `ts-arch`） |

**请求体类型**（`SkillUpdatePayload`）：

```typescript
// src/types.ts 新增
export interface SkillUpdatePayload {
  title?:       string;      // 标题
  description?: string;      // 一句话描述
  category?:    Category;    // 分类（下拉选择）
  tags?:        string[];    // 标签（增删 tag）
  details?: {
    whenToUse?:   string[];  // 适用场景列表
    rawContent?:  string;    // 原始文件内容（编辑此字段 = 整个文件内容重写）
  };
}
```

**返回格式**：

```typescript
// HTTP 200 — 保存成功
{
  "success": true,
  "data": Skill  // 返回更新后的完整 Skill 对象
}

// HTTP 400 — 字段校验失败
{
  "success": false,
  "error": "category must be one of: 编程开发, 内容创作, ..."
}

// HTTP 500 — 文件写入失败
{
  "success": false,
  "error": "Failed to write file: ~/.codex/skills/coding/ts-arch.md"
}
```

**后端写回逻辑说明**（Express 真实接入时）：

```
1. 读取 sourcePath 对应的原始 .md 文件
2. 解析 frontmatter 与正文
3. 用 payload 字段覆盖 frontmatter 中对应的 key
4. 如果传了 rawContent，则用 rawContent 完整替换整个文件内容
5. 写回文件（fs.writeFile）
6. 重新解析该文件，返回更新后的 Skill 对象
```

> **注意**：`rawContent` 与其他字段互斥建议分开处理——一般编辑只改 frontmatter，`rawContent` 编辑相当于直接改文件，风险较高，前端应加确认提示。

---

#### `POST /api/scan`

触发一次本地文件系统扫描，重新读取配置的目录。

**请求体**（可选，留空则用默认配置）：

```typescript
{
  "paths": string[]  // 可选，临时覆盖默认扫描路径
}
```

**返回格式**：

```typescript
// HTTP 200 — 扫描完成
{
  "success": true,
  "data": {
    "success": true,
    "totalFound": 23,
    "newCount": 2,
    "updatedCount": 1,
    "failedCount": 0,
    "scannedAt": "2026-04-13T10:05:00Z",
    "errors": []
  }
}

// HTTP 500 — 扫描出错
{
  "success": false,
  "error": "Permission denied: ~/.codex/skills/"
}
```

---

#### `GET /api/config` / `PUT /api/config`

读取或更新 App 配置文件（`skill-dashboard.config.json`）。

**GET 返回**：

```typescript
{
  "success": true,
  "data": {
    "scanPaths": ["~/.codex/skills/", "~/Documents/prompts/"],
    "autoScan": true,
    "lastScanAt": "2026-04-13T10:00:00Z"
  }
}
```

**PUT 请求体**：

```typescript
{
  "scanPaths": ["~/.codex/skills/"],
  "autoScan": false
}
```

---

## 三、Mock 数据层设计

### 3.1 Mock 数据文件结构

```
src/
├── mock/
│   ├── skills.ts          ← 完整 mock skills 数据（替换 constants.ts 的 SKILLS）
│   ├── stats.ts           ← mock 统计数据
│   └── index.ts           ← 统一导出 + mock 请求函数
├── api/
│   ├── client.ts          ← 真实 API 请求封装（fetch）
│   └── index.ts           ← 开关：Mock 模式 or 真实模式
```

### 3.2 Mock 开关逻辑

```typescript
// src/api/index.ts

const USE_MOCK = import.meta.env.VITE_USE_MOCK === 'true' || true; // 默认 Mock

export const api = USE_MOCK ? mockApi : realApi;
```

```
# .env.example
VITE_USE_MOCK=true        # true = 使用 mock，false = 接真实后端
VITE_API_BASE_URL=http://localhost:3001
```

### 3.3 编辑功能 UX 状态机

详情面板有两种状态，通过右上角按钮切换：

```
┌──────────────────────────────────────────┐
│  [← 关闭]              [✏ 编辑] [⧉ 复制] │  ← 查看模式（默认）
├──────────────────────────────────────────┤
│  标题（只读文字）                          │
│  [分类标签] [状态标签]                    │
│  ...                                     │
└──────────────────────────────────────────┘

点击「编辑」按钮后：

┌──────────────────────────────────────────┐
│  [← 取消]              [💾 保存中...] │  ← 编辑模式
├──────────────────────────────────────────┤
│  [__标题输入框___________________]        │  ← text input
│  [分类下拉 ▼]  [状态标签]               │  ← select
│  ─────────────────────────────────────   │
│  DESCRIPTION                             │
│  [___描述 textarea________________]      │  ← textarea (2行)
│  ─────────────────────────────────────   │
│  TAGS                                    │
│  [写作 ×] [总结 ×] [+ 添加标签]          │  ← tag chips + input
│  ─────────────────────────────────────   │
│  WHEN TO USE                             │
│  • [___场景输入框___] [×]               │  ← 可增删的 bullet list
│  • [___场景输入框___] [×]               │
│  [+ 添加场景]                            │
│  ─────────────────────────────────────   │
│  RAW CONTENT                             │
│  ┌─────────────────────────────────────┐ │
│  │ [代码编辑区 · 可直接修改]            │ │  ← textarea with mono font
│  └─────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

**各字段编辑组件映射**：

| 字段 | 编辑组件 | 校验规则 |
|------|---------|---------|
| `title` | 单行 `<input type="text">` | 必填，最长 60 字 |
| `description` | 多行 `<textarea>` (2行) | 必填，最长 100 字 |
| `category` | `<select>` 下拉，7 个选项 | 必须是枚举值 |
| `tags` | Tag chip 列表 + 输入框（回车添加，× 删除） | 每个 tag 最长 20 字，最多 10 个 |
| `whenToUse` | 可增删的 bullet list（每条一个 input） | 每条最长 80 字，最多 8 条 |
| `rawContent` | 等宽字体 `<textarea>`，高度自适应 | 无强制校验，保存前提示"将覆盖原文件" |

**编辑模式状态变量**（前端 React state）：

```typescript
// 在详情面板组件中新增
interface EditState {
  isEditing: boolean;          // 是否处于编辑模式
  isSaving: boolean;           // 保存请求进行中
  draft: SkillUpdatePayload;   // 当前编辑中的草稿（与 skill 原数据分离）
  errors: Partial<Record<keyof SkillUpdatePayload, string>>; // 字段校验错误
}
```

**编辑流程**：

```
1. 点击「编辑」→ isEditing = true，draft = 拷贝当前 skill 字段值
2. 用户修改各字段 → 实时更新 draft（不影响原数据）
3. 点击「保存」→ isSaving = true，调用 mockApi.updateSkill(id, draft)
4a. 成功 → 更新全局 skills 列表中该 skill，isEditing = false，显示成功 toast
4b. 失败 → isSaving = false，显示错误 toast，保持编辑态
5. 点击「取消」→ isEditing = false，draft 丢弃
```

---

### 3.4 Mock 请求函数签名

```typescript
// src/mock/index.ts

export const mockApi = {
  // 模拟 GET /api/skills
  getSkills: async (params?: {
    q?: string;
    category?: Category;
  }): Promise<ApiResponse<{ skills: Skill[]; total: number }>> => {
    await delay(120); // 模拟网络延迟
    let result = [...MOCK_SKILLS];
    if (params?.category) result = result.filter(s => s.category === params.category);
    if (params?.q) result = result.filter(s =>
      s.title.includes(params.q!) ||
      s.description.includes(params.q!) ||
      s.tags.some(t => t.includes(params.q!))
    );
    return { success: true, data: { skills: result, total: result.length } };
  },

  // 模拟 GET /api/skills/:id
  getSkillById: async (id: string): Promise<ApiResponse<Skill>> => {
    await delay(80);
    const skill = MOCK_SKILLS.find(s => s.id === id);
    if (!skill) return { success: false, data: null!, error: `Skill not found: ${id}` };
    return { success: true, data: skill };
  },

  // 模拟 GET /api/stats
  getStats: async (): Promise<ApiResponse<StatsData>> => {
    await delay(50);
    return { success: true, data: MOCK_STATS };
  },

  // 模拟 PUT /api/skills/:id（编辑保存）
  updateSkill: async (
    id: string,
    payload: SkillUpdatePayload
  ): Promise<ApiResponse<Skill>> => {
    await delay(400); // 模拟文件写入耗时
    const idx = MOCK_SKILLS.findIndex(s => s.id === id);
    if (idx === -1) return { success: false, data: null!, error: `Skill not found: ${id}` };

    // 合并 payload 到 mock 数据（浅合并 + details 深合并）
    const updated: Skill = {
      ...MOCK_SKILLS[idx],
      ...(payload.title       && { title: payload.title }),
      ...(payload.description && { description: payload.description }),
      ...(payload.category    && { category: payload.category }),
      ...(payload.tags        && { tags: payload.tags }),
      details: {
        ...MOCK_SKILLS[idx].details,
        ...(payload.details?.whenToUse  && { whenToUse: payload.details.whenToUse }),
        ...(payload.details?.rawContent && { rawContent: payload.details.rawContent }),
      },
      updatedAt: new Date().toISOString(),
    };
    MOCK_SKILLS[idx] = updated; // 更新内存中的 mock 数据
    return { success: true, data: updated };
  },

  // 模拟 POST /api/scan（假扫描，只更新时间）
  triggerScan: async (): Promise<ApiResponse<ScanResult>> => {
    await delay(1500); // 模拟扫描耗时
    return {
      success: true,
      data: {
        success: true,
        totalFound: MOCK_SKILLS.length,
        newCount: 0,
        updatedCount: 0,
        failedCount: 0,
        scannedAt: new Date().toISOString(),
        errors: [],
      }
    };
  },
};
```

### 3.4 完整 Mock Skills 数据（对照 UI 设计文档分类）

```typescript
// src/mock/skills.ts

import { Skill } from '../types';

export const MOCK_SKILLS: Skill[] = [
  // ── 编程开发 (3) ──
  {
    id: 'ts-arch',
    title: 'TypeScript 架构师',
    description: '系统级架构规划和复杂类型重构代理。',
    category: '编程开发',
    tags: ['typescript', 'architecture', 'refactor'],
    status: 'active',
    icon: 'Code2',
    sourcePath: '~/.codex/skills/coding/ts-arch.md',
    fileName: 'ts-arch.md',
    fileType: 'md',
    updatedAt: '2026-04-10T08:30:00Z',
    scannedAt: '2026-04-13T10:00:00Z',
    details: {
      whatItDoes: '分析复杂的 TypeScript 代码库，提出架构改进建议、设计模式实现和严格的类型安全强制执行。',
      whenToUse: ['重构遗留逻辑', '初始项目脚手架', '代码质量审计'],
      triggerWords: ['refactor', 'typescript', 'architecture', '架构', '重构'],
      rawContent: '# TypeScript 架构师\n\n## 描述\n...',
    },
  },
  {
    id: 'infra-expert',
    title: '基础设施专家',
    description: 'AWS / Kubernetes 配置优化及故障排除。',
    category: '编程开发',
    tags: ['aws', 'kubernetes', 'devops', 'terraform'],
    status: 'active',
    icon: 'Server',
    sourcePath: '~/.codex/skills/coding/infra-expert.md',
    fileName: 'infra-expert.md',
    fileType: 'md',
    updatedAt: '2026-04-08T14:20:00Z',
    scannedAt: '2026-04-13T10:00:00Z',
    details: {
      whatItDoes: '为现代 DevOps 环境提供云基础设施设置、优化和安全最佳实践的专家指导。',
      whenToUse: ['云架构设计', 'Kubernetes 故障排除', '成本优化'],
      triggerWords: ['aws', 'k8s', 'terraform', 'deploy', '部署'],
      rawContent: '# 基础设施专家\n\n## 描述\n...',
    },
  },
  {
    id: 'code-reviewer',
    title: '代码审查员',
    description: '提供全面的 PR 代码审查和安全漏洞扫描。',
    category: '编程开发',
    tags: ['code review', 'security', 'best practices'],
    status: 'active',
    icon: 'GitPullRequest',
    sourcePath: '~/.codex/skills/coding/code-reviewer.md',
    fileName: 'code-reviewer.md',
    fileType: 'md',
    updatedAt: '2026-04-11T09:00:00Z',
    scannedAt: '2026-04-13T10:00:00Z',
    details: {
      whatItDoes: '对代码变更进行全面审查，识别 Bug、安全漏洞、性能问题和不符合最佳实践的代码。',
      whenToUse: ['PR 审查', '安全审计', '新成员代码检查'],
      triggerWords: ['review', 'PR', 'code review', '代码审查'],
      rawContent: '# 代码审查员\n\n## 描述\n...',
    },
  },

  // ── 商业营销 (1) + 内容创作 (2) ──
  {
    id: 'marketing-copy',
    title: '营销文案 AI',
    description: '高转化率的落地页文案和社交媒体内容生成器。',
    category: '商业营销',
    tags: ['marketing', 'copywriting', 'social media'],
    status: 'active',
    icon: 'Megaphone',
    sourcePath: '~/.codex/skills/writing/marketing-copy.md',
    fileName: 'marketing-copy.md',
    fileType: 'md',
    updatedAt: '2026-04-09T16:45:00Z',
    scannedAt: '2026-04-13T10:00:00Z',
    details: {
      whatItDoes: '针对特定受众和平台生成极具说服力的营销文案，专注于转化率和互动率。',
      whenToUse: ['落地页创建', '社交媒体活动', '邮件营销'],
      triggerWords: ['marketing', 'copy', 'landing page', '文案', '营销'],
      rawContent: '# 营销文案 AI\n\n## 描述\n...',
    },
  },
  {
    id: 'data-summarizer',
    title: '数据摘要',
    description: '将大型数据集提炼为可操作的见解和核心主题。',
    category: '数据分析',
    tags: ['summary', 'analysis', 'report'],
    status: 'active',
    icon: 'FileText',
    sourcePath: '~/.codex/skills/writing/data-summarizer.md',
    fileName: 'data-summarizer.md',
    fileType: 'md',
    updatedAt: '2026-04-07T11:30:00Z',
    scannedAt: '2026-04-13T10:00:00Z',
    details: {
      whatItDoes: '处理大量文本或结构化数据，提取关键发现、趋势和执行摘要。',
      whenToUse: ['市场研究分析', '会议纪要提炼', '文档审查'],
      triggerWords: ['summarize', 'summary', '总结', '摘要'],
      rawContent: '# 数据摘要\n\n## 描述\n...',
    },
  },
  {
    id: 'cold-email',
    title: '冷邮件写手',
    description: '为陌生人触达场景生成高开率的个性化冷邮件。',
    category: '商业营销',
    tags: ['email', 'outreach', 'sales'],
    status: 'active',
    icon: 'Mail',
    sourcePath: '~/.codex/skills/writing/cold-email.md',
    fileName: 'cold-email.md',
    fileType: 'md',
    updatedAt: '2026-04-12T07:15:00Z',
    scannedAt: '2026-04-13T10:00:00Z',
    details: {
      whatItDoes: '根据目标对象背景生成高度个性化的冷邮件，提高开信率和回复率。',
      whenToUse: ['BD 触达', '求职联系', '合作邀约'],
      triggerWords: ['cold email', 'outreach', '冷邮件', '陌生人'],
      rawContent: '# 冷邮件写手\n\n## 描述\n...',
    },
  },

  // ── 效率流程 (2) ──
  {
    id: 'sop-runner',
    title: 'SOP 执行器',
    description: '以精准验证执行多步骤技术工作流。',
    category: '效率流程',
    tags: ['workflow', 'automation', 'process'],
    status: 'active',
    icon: 'Terminal',
    sourcePath: '~/.codex/skills/sops/sop-runner.md',
    fileName: 'sop-runner.md',
    fileType: 'md',
    updatedAt: '2026-04-05T13:00:00Z',
    scannedAt: '2026-04-13T10:00:00Z',
    details: {
      whatItDoes: '通过遵循预定义的 SOP 并内置验证步骤，自动化复杂的技术流程。',
      whenToUse: ['基础设施部署', '数据库迁移', '安全审计'],
      triggerWords: ['SOP', 'workflow', 'checklist', '流程'],
      rawContent: '# SOP 执行器\n\n## 描述\n...',
    },
  },
  {
    id: 'onboarding-sop',
    title: '新人入职 SOP',
    description: '标准化的团队新成员入职流程检查清单。',
    category: '效率流程',
    tags: ['onboarding', 'HR', 'checklist'],
    status: 'unrecognized',
    icon: 'UserPlus',
    sourcePath: '~/.codex/skills/sops/onboarding-sop.txt',
    fileName: 'onboarding-sop.txt',
    fileType: 'txt',
    updatedAt: '2026-03-20T10:00:00Z',
    scannedAt: '2026-04-13T10:00:00Z',
    details: {
      whatItDoes: '（元信息未能自动解析，请手动补充描述）',
      whenToUse: [],
      triggerWords: [],
      rawContent: 'Day 1: 账号开通\nDay 2: 系统权限...',
    },
  },

  // ── 内容创作 (1) ──
  {
    id: 'image-forge',
    title: '图像锻造',
    description: '生成生产级 UI 资产和矢量风格插图。',
    category: '内容创作',
    tags: ['image generation', 'UI', 'illustration'],
    status: 'updating',
    icon: 'Palette',
    sourcePath: '~/.codex/skills/images/image-forge.md',
    fileName: 'image-forge.md',
    fileType: 'md',
    updatedAt: '2026-04-13T06:00:00Z',
    scannedAt: '2026-04-13T10:00:00Z',
    details: {
      whatItDoes: '为用户界面创建高质量的视觉资产，包括图标、插图和布局原型。',
      whenToUse: ['UI 设计原型', '品牌资产生成', '演示视觉效果'],
      triggerWords: ['image', 'generate', 'illustration', '图像', '插图'],
      rawContent: '# 图像锻造\n\n## 描述\n...',
    },
  },

  // ── 产品设计 (1) + 数据分析 (1) ──
  {
    id: 'ux-analyst',
    title: 'UX 分析师',
    description: '用户体验审查和可用性问题识别专家。',
    category: '产品设计',
    tags: ['UX', 'usability', 'design review'],
    status: 'active',
    icon: 'Eye',
    sourcePath: '~/.codex/skills/analysis/ux-analyst.md',
    fileName: 'ux-analyst.md',
    fileType: 'md',
    updatedAt: '2026-04-06T15:30:00Z',
    scannedAt: '2026-04-13T10:00:00Z',
    details: {
      whatItDoes: '系统性地评估界面设计，识别可用性问题、信息架构缺陷和用户旅程摩擦点。',
      whenToUse: ['设计评审', '可用性测试准备', '竞品分析'],
      triggerWords: ['UX', 'usability', 'design review', '用户体验'],
      rawContent: '# UX 分析师\n\n## 描述\n...',
    },
  },
  {
    id: 'market-research',
    title: '市场调研助手',
    description: '快速完成竞品分析和市场定位研究。',
    category: '商业营销',
    tags: ['market research', 'competitive analysis', 'strategy'],
    status: 'active',
    icon: 'TrendingUp',
    sourcePath: '~/.codex/skills/analysis/market-research.md',
    fileName: 'market-research.md',
    fileType: 'md',
    updatedAt: '2026-04-04T12:00:00Z',
    scannedAt: '2026-04-13T10:00:00Z',
    details: {
      whatItDoes: '快速梳理市场格局、竞争对手定位、用户需求痛点，输出结构化调研报告。',
      whenToUse: ['新产品立项', '竞品分析', '市场进入策略'],
      triggerWords: ['market research', 'competitive', '市场调研', '竞品'],
      rawContent: '# 市场调研助手\n\n## 描述\n...',
    },
  },
];
```

---

### 3.5 Mock Stats 数据

```typescript
// src/mock/stats.ts

import { StatsData } from '../types';

export const MOCK_STATS: StatsData = {
  totalSkills: 10,
  totalCategories: 6,   // 编程开发/内容创作/数据分析/产品设计/效率流程/商业营销
  newSinceLastScan: 2,
  lastScanTime: '3分钟前',
};
```

---

## 四、本地文件解析规范（真实接口用）

### 4.1 支持的文件格式

#### Markdown 格式（推荐）

```markdown
---
id: ts-arch
title: TypeScript 架构师
description: 系统级架构规划和复杂类型重构代理。
category: 编程
tags: [typescript, architecture, refactor]
icon: Code2
when_to_use:
  - 重构遗留逻辑
  - 初始项目脚手架
trigger_words: [refactor, typescript, 架构, 重构]
---

## 这个 Skill 能做什么

分析复杂的 TypeScript 代码库，提出架构改进建议...

## 原始内容

（此行以下为 skill 正文，会被放入 rawContent）
```

#### JSON 格式（Codex Skill 体系兼容）

```json
{
  "skill_id": "ts-arch",
  "title": "TypeScript 架构师",
  "description": "系统级架构规划",
  "category": "编程",
  "tags": ["typescript", "architecture"],
  "when_to_use": ["重构遗留逻辑"],
  "trigger_words": ["refactor"]
}
```

### 4.2 解析失败的兜底策略

| 情况 | 处理方式 |
|------|---------|
| 没有 frontmatter | title = 文件名去后缀，description = 文件前50字，status = `unrecognized` |
| category 不在枚举中 | 归入 `其他` |
| 没有 tags | tags = [] |
| 文件读取权限失败 | 跳过，记录到 ScanResult.errors |

---

## 五、数据流向总结

```
┌─────────────────────────────────────────────────────────┐
│                    数据流向总图                            │
│                                                          │
│  Phase 1 (当前 Demo)                                     │
│  ─────────────────                                       │
│  src/mock/skills.ts ──→ mockApi.getSkills() ──→ App.tsx  │
│                          (假延迟 120ms)                   │
│                                                          │
│  Phase 2 (接真实后端)                                     │
│  ─────────────────────                                   │
│  .codex/skills/*.md                                      │
│       ↓ (Express /api/scan)                              │
│  解析成 Skill[] ──→ 内存缓存                               │
│       ↓ (Express /api/skills)                            │
│  realApi.getSkills() ──→ App.tsx                         │
│                                                          │
│  切换方式：VITE_USE_MOCK=false                             │
└─────────────────────────────────────────────────────────┘
```

### 功能模块 × 数据方式对照

| 功能模块 | MVP 数据方式 | 真实接入方式 | 优先级 |
|---------|------------|------------|-------|
| Skill 卡片列表展示 | Mock (constants.ts) | `GET /api/skills` | P0 |
| 关键词搜索 | 前端过滤 Mock 数组 | Query Param `?q=` | P0 |
| 分类筛选 | 前端过滤 Mock 数组 | Query Param `?category=` | P0 |
| 侧边栏分类计数 | 前端 reduce 计算 | 同列表接口聚合 | P0 |
| Hero 统计数字 | Mock Stats 对象 | `GET /api/stats` | P0 |
| Skill 详情面板（查看） | Mock 数据内 details 字段 | `GET /api/skills/:id` | P0 |
| 原始内容预览 | Mock rawContent | 文件读取内容 | P0 |
| **Skill 字段编辑（编辑模式）** | **mockApi.updateSkill 更新内存** | **`PUT /api/skills/:id` + fs.writeFile** | **P1** |
| 重新扫描按钮 | Mock 假扫描（延迟后刷新） | `POST /api/scan` | P1 |
| 设置（扫描路径） | 静态 .env | `GET/PUT /api/config` | P2 |

---

## 六、`types.ts` 与 `constants.ts` 改造建议

### 现有问题

| 问题 | 位置 | 改造方向 |
|------|------|---------|
| `Category` 类型不匹配真实生态 | `types.ts:1` | 改为 7 分类：编程开发/内容创作/数据分析/产品设计/效率流程/商业营销/其他 |
| `status` 只有 `就绪/更新中`（中文） | `types.ts:8` | 改为 `active/unrecognized/updating` |
| `Skill` 缺少 `tags`、`fileName`、`fileType` | `types.ts` | 新增字段 |
| `details.image` 字段在 UI 设计中无对应 | `types.ts:15` | 删除，用 `icon` 代替 |
| `STATS` 数组格式与 `StatsData` 接口不对齐 | `constants.ts:96` | 改为 `StatsData` 单对象 |
| Mock 数据只有 6 条，不足以展示网格布局 | `constants.ts` | 扩充到 10 条，覆盖所有分类 |

---

> **下一步**：按照本文档的类型定义重写 `src/types.ts`，把 mock 数据搬进 `src/mock/skills.ts`，并新建 `src/api/index.ts` 作为统一请求入口。详情面板和搜索筛选逻辑均可直接调 `mockApi`，后续切真实后端只需改一个环境变量。

---

## 七、我的设计建议

> 以下是基于整个产品定位、当前代码状态和你的目标（面试作品集 + 真实可用）综合给出的判断，供你参考或直接采用。

---

### 7.1 开发顺序建议

**不要按功能模块顺序开发，要按"能跑起来给人看"的最短路径推进**：

```
第 1 步（1-2天）：重写类型 + Mock 数据
  └─ src/types.ts 换成本文档版本
  └─ src/mock/skills.ts 放入 10 条数据（覆盖 6 个分类）
  └─ src/api/index.ts 封装 mockApi
  └─ 验收标准：卡片网格正常显示，侧边栏计数正确

第 2 步（1天）：接通详情面板
  └─ 点击卡片 → 右侧面板展示完整 Skill 信息
  └─ 验收标准：所有字段都能显示，rawContent 代码预览区正常滚动

第 3 步（1-2天）：搜索 + 筛选
  └─ 关键词搜索（前端过滤）
  └─ 分类 Filter Chip（前端过滤）
  └─ 验收标准：搜索"typescript"能找到对应 skill，切分类后计数变化

第 4 步（1-2天）：编辑功能
  └─ 详情面板加「编辑」按钮，切换编辑模式
  └─ 各字段换成对应的输入组件
  └─ 调 mockApi.updateSkill，保存后卡片实时更新
  └─ 验收标准：改标题后卡片立即反映，刷新页面后（Mock）恢复原值

第 5 步（按需）：接真实后端
  └─ VITE_USE_MOCK=false
  └─ Express 实现文件扫描 + 读写接口
```

---

### 7.2 编辑功能的关键设计决策

**建议：`rawContent` 编辑框默认折叠，需要展开才能编辑**

原因：`rawContent` 是整个文件的内容，如果和其他字段并排放在同一个编辑面板里，视觉上会喧宾夺主，且用户误操作风险高。建议做成这样：

```
原始内容 (Skill 源文件)           [展开编辑 ▼]
┌─────────────────────────────────────────┐
│ # TypeScript 架构师                      │  ← 查看模式：只读代码预览
│ ...                                      │
└─────────────────────────────────────────┘
```

点击「展开编辑」后，代码预览区变为可编辑 textarea，顶部加一行警告：

> ⚠️ 直接编辑原始内容将覆盖文件，建议先备份。

---

**建议：tags 输入用「回车确认」+「退格删除最后一个」的交互**

比加号按钮更流畅，参考 Linear、Notion 的 tag 输入体验。实现很简单：

```typescript
onKeyDown={(e) => {
  if (e.key === 'Enter' && inputValue.trim()) {
    addTag(inputValue.trim());
    setInputValue('');
  }
  if (e.key === 'Backspace' && inputValue === '' && tags.length > 0) {
    removeLastTag();
  }
}}
```

---

**建议：编辑保存成功后，在右侧面板顶部短暂出现一条 toast，而不是弹出 Modal**

保存成功 → 面板顶部滑入一条绿色 toast（2秒后自动消失），用户停留在当前位置继续操作，不打断流程。失败时 toast 变红，且不自动消失，直到用户手动关闭。

---

### 7.3 数据结构上的一个潜在问题

**`id` 字段依赖文件名，重命名文件会导致 ID 变化，引发问题。**

当前设计：`id = 文件名 slug`（如 `ts-arch.md` → `ts-arch`）

这在 Mock 阶段没问题，但真实接入后，如果用户重命名了文件，Dashboard 里所有对该 skill 的引用（比如"最近编辑"记录）都会失效。

**我的建议**：在第一次扫描到该文件时，生成一个稳定的 UUID 并写入 frontmatter：

```yaml
---
id: a1b2c3d4   # 由 Dashboard 首次扫描时生成，后续不变
title: TypeScript 架构师
...
---
```

文件重命名后，只要 frontmatter 里的 `id` 不变，Dashboard 就能持续追踪。这个改动在 MVP 阶段可以先跳过，但要预留字段位置，真实接入时加上。

---

### 7.4 哪些功能可以先做成假的，用户感知不到

| 功能 | 假做法 | 用户感知 |
|------|--------|---------|
| 「重新扫描」按钮 | 点击后 loading 1.5 秒，toast 提示"扫描完成，发现 10 个 skill" | 感知不到，体验完整 |
| `updatedAt` 时间显示 | Mock 数据里写死相对时间字符串（"3天前"） | 感知不到 |
| Hero 区"新增 2 个" | Mock Stats 里写死 `newSinceLastScan: 2` | 感知不到 |
| 编辑保存 | 只更新内存，刷新丢失 | **能感知**，面试演示时不要刷新页面即可 |
| `sourcePath` 路径 | 写合理的假路径（如 `~/.codex/skills/coding/ts-arch.md`） | 感知不到，路径看起来真实就行 |

---

### 7.5 面试演示脚本建议

如果你要用这个产品做面试作品集展示，建议准备以下演示路径（约 3 分钟）：

```
1. 打开 Dashboard，讲解 Hero 区（"这是我的 AI 能力总览，23 个 skill，6 个分类"）
2. 演示搜索（搜"代码"，结果实时过滤）
3. 点击分类筛选（切到"编程开发"，卡片重新排布）
4. 点击一张卡片，打开详情面板（讲解各区块内容）
5. 点击「编辑」，修改描述文字，点保存，卡片实时更新
6. 讲扩展性："现在是 Mock 数据，真实版本会扫描本地 .codex/skills/ 目录，
   通过 Express API 读写原始 Markdown 文件的 frontmatter"
```

这个路径能完整展示：数据展示 → 交互 → 编辑能力 → 技术深度（不只是写 UI）。
