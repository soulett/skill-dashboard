# Skill Dashboard - Alpha 真实链路设计说明

> 版本：v0.2 Alpha Draft
> 日期：2026-04-19
> 目标：从静态 Demo 进入真实可用的本地闭环版本

---

## 1. Alpha 目标

这一版的目标不是继续打磨“更好看的 Demo”，而是把产品推进成一个真正可用的本地工具。

本版只聚焦一条完整闭环：

`扫描本地技能 -> 展示真实数据 -> 健康检查发现缺口 -> 去完善 -> 保存补充信息`

用户完成这条链路后，应该得到一个明确结果：

- 能看到自己真实安装了哪些技能
- 系统能指出哪些技能信息不完整
- 可以直接补齐关键信息
- 补完后再次打开仍然保留

---

## 2. 本版交付范围

### 2.1 必须交付

1. 扫描指定本地技能目录，识别 skill 文件
2. 解析出统一的 `Skill` 数据结构并展示到前端
3. 基于真实数据运行健康检查
4. 从健康检查页跳到技能详情并进入编辑
5. 把补充信息保存到 sidecar metadata，并在下次扫描后继续生效

### 2.2 明确不做

1. 不直接改原始 `SKILL.md`
2. 不做云同步、多端同步、账号体系
3. 不做技能执行或调用 LLM
4. 不做市场下载后的复杂安装管理
5. 不做推荐系统和使用统计闭环
6. 不追求兼容所有历史 skill 格式，只支持明确的首批扫描规则

### 2.3 成功标准

1. 接入一个真实 skill 目录后，首页能展示真实技能卡片
2. 健康检查能指出某个 skill 缺少哪些字段
3. 点击“去完善”能进入对应技能的编辑态
4. 保存后刷新页面，补充信息不会丢
5. 原始 skill 文件不被破坏

---

## 3. 用户完整链路

### 3.1 用户主路径

1. 用户打开产品，系统自动扫描本地技能目录
2. 首页展示真实技能卡片、分类数量和最近扫描时间
3. 用户进入 `健康检查`
4. 系统列出信息不完整的技能，并标出缺失项
5. 用户点击 `去完善`
6. 页面跳回技能库，并自动打开该技能的右侧详情面板，进入编辑态
7. 用户补齐描述、分类、标签、适用场景等字段
8. 用户点击保存
9. sidecar metadata 写入成功
10. 右侧详情、技能卡片、健康分数同步刷新

### 3.2 页面状态流转

建议把这一版状态切成 4 个明确阶段：

1. `扫描中`
   - 首页显示扫描状态或 loading
   - 避免误导性的空列表结论

2. `已展示`
   - 首页卡片、分类、统计都来自真实扫描结果
   - 右侧详情默认关闭
   - 健康检查可进入

3. `待完善`
   - 健康检查显示某个 skill 的缺字段
   - 用户点击 `去完善`
   - 跳转后自动选中 skill 并进入编辑态

4. `已保存`
   - sidecar metadata 写入成功
   - 详情、卡片、健康检查结果同步刷新

### 3.3 关键交互规则

1. “打开详情”和“打开并编辑”是两种不同动作
2. 自动编辑态只能消费一次
3. 保存成功后，前端展示数据必须立即更新
4. 健康检查结果必须基于合并后的最终数据
5. 关闭右侧面板不应影响已保存内容

---

## 4. 数据来源与存储策略

这一版的数据由两层来源合并得到：

### 4.1 原始技能文件

提供基础事实：

- 文件是否存在
- 文件路径
- 文件名
- 原始内容
- 文件更新时间
- 能从内容中直接解析出的标题、描述、分类、标签等

### 4.2 Sidecar Metadata

提供产品层补充和修正：

- 用户手动补的描述
- 用户修正后的分类
- 用户补充的标签
- 用户补充的适用场景
- 后续扩展字段，例如人工备注、健康检查标记

最终返回给前端的数据是：

`原始解析结果 + sidecar metadata 覆盖后的最终 Skill`

### 4.3 为什么这版不直接改原文件

1. 原始 skill 文件格式不稳定
2. 原始内容可能同时被别的工具或工作流使用
3. Alpha 的目标是形成闭环，不是做通用文件编辑器
4. sidecar 方案更稳，也给后续“直接写回原文件”预留升级空间

---

## 5. Sidecar Metadata 设计

### 5.1 Sidecar Metadata 是什么

可以把 sidecar metadata 理解成一份“跟在原 skill 文件旁边的补充档案”。

- 原始 `SKILL.md` 负责记录原始能力定义
- `metadata.json` 负责记录产品视角下的整理结果

它不是替代原文件，而是在不破坏原文件的前提下，为产品增加一层可维护、可编辑、可持久化的数据层。

### 5.2 为什么先用一个总文件

Alpha 阶段推荐先使用一个总文件，而不是每个 skill 单独一个 metadata 文件。

推荐原因：

1. 实现简单
2. 读写成本低
3. 调试方便
4. 便于扫描后一次性合并
5. 适合当前单用户、本地工具的使用场景

### 5.3 文件放置位置

Alpha 阶段建议使用应用自己的数据目录，而不是污染原 skill 仓库。

推荐路径：

`skill-dashboard-data/metadata.json`

如果需要放在项目目录下便于调试，开发期可以先落在：

`D:\AI-Coding\skill dashboard\skill-dashboard-data\metadata.json`

后续如果产品独立运行，再迁移到专门的用户数据目录。

### 5.4 文件创建策略

`metadata.json` 不是一开始就必须装满，而是一个按需生长的文件。

创建逻辑建议如下：

1. 应用启动后，扫描配置中的数据目录
2. 如果 `skill-dashboard-data` 目录不存在，则先创建目录
3. 如果 `metadata.json` 不存在，则自动创建一个空文件
4. 初始化内容如下：

```json
{
  "version": 1,
  "updatedAt": "2026-04-19T12:00:00.000Z",
  "skills": {}
}
```

5. 当用户第一次编辑某个 skill 并点击保存时，才真正往 `skills` 下写入该 skill 的补充数据

也就是说：

- 扫描阶段负责“确保 metadata 容器存在”
- 编辑保存阶段负责“按需写入具体 skill 条目”

### 5.5 文件结构建议

推荐结构：

```json
{
  "version": 1,
  "updatedAt": "2026-04-19T12:00:00.000Z",
  "skills": {
    "coding/git-ops/SKILL.md": {
      "description": "处理提交、分支、冲突和回滚建议，让日常 Git 操作更稳更快。",
      "category": "编程开发",
      "tags": ["git", "workflow", "review"],
      "whenToUse": [
        "提交前检查",
        "处理冲突时"
      ]
    }
  }
}
```

字段说明：

- `version`：metadata 文件版本，后续迁移用
- `updatedAt`：最近一次写入时间
- `skills`：以 skill 稳定标识为 key 的补充信息映射

### 5.6 Skill 标识策略

Alpha 阶段不建议只用文件名作为 key，因为重名风险高。

建议使用：

`skillId = 扫描根目录下的相对路径`

例如：

- `coding/git-ops/SKILL.md`
- `writing/cold-email/SKILL.md`

这样做的优点：

1. 生成简单
2. 可读性强
3. 不需要修改原文件
4. 在文件不移动的前提下足够稳定

后续如果要支持重命名和移动文件，再考虑引入真正的持久 ID。

### 5.7 什么时候写入

只有用户明确点击保存时才写入 metadata。

写入流程建议如下：

1. 读取现有 `metadata.json`
2. 找到当前 skill 对应 key
3. 用本次保存的数据覆盖该 key 下的字段
4. 更新根级 `updatedAt`
5. 将完整对象原子性写回磁盘

### 5.8 什么时候读取并合并

后端在返回给前端前完成合并，不把合并逻辑丢给前端。

建议流程：

1. 扫描原始 skill 文件
2. 解析出基础 `Skill`
3. 读取 `metadata.json`
4. 查找该 skill 是否存在补充信息
5. 按字段覆盖
6. 返回最终 `Skill`

字段覆盖优先级：

`sidecar metadata > 原始解析结果 > 文件名兜底推断`

### 5.9 为什么这个方案适合 Alpha

因为它能让产品先实现完整闭环：

- 扫描真实资产
- 展示真实结果
- 发现缺口
- 补充信息
- 持久化保存

同时又避免了第一版就进入“改坏原始 skill 文件”的高风险区域。

---

## 6. 数据模型最小约定

Alpha 阶段最小 `Skill` 结构建议包含：

```ts
type Category =
  | '编程开发'
  | '内容创作'
  | '数据分析'
  | '产品设计'
  | '效率流程'
  | '商业营销'
  | '其他';

interface Skill {
  id: string;
  title: string;
  description: string;
  category: Category;
  tags: string[];
  sourcePath: string;
  fileName: string;
  updatedAt: string;
  scannedAt: string;
  status: 'active' | 'unrecognized' | 'updating';
  details: {
    whatItDoes: string;
    whenToUse: string[];
    rawContent: string;
  };
}
```

Alpha 阶段最小 metadata patch 结构：

```ts
interface SkillMetadataPatch {
  description?: string;
  category?: Category;
  tags?: string[];
  whenToUse?: string[];
}
```

---

## 7. 最小 API 约定

这一版只需要最小 4 个接口：

1. `GET /api/skills`
   - 返回合并后的 skill 列表

2. `GET /api/stats`
   - 返回统计信息

3. `POST /api/scan`
   - 触发重新扫描

4. `PATCH /api/skills/:id/metadata`
   - 写入某个 skill 的 sidecar metadata

推荐不要在 Alpha 阶段做直接写回原文件的 `PUT /api/skills/:id`。

---

## 8. 开发顺序建议

建议按下面顺序推进，而不是并行发散：

### 阶段 1：扫描与解析

目标：

- 扫描本地目录
- 识别 skill 文件
- 产出统一 `Skill[]`
- 自动创建空的 `metadata.json`

### 阶段 2：读取并合并 metadata

目标：

- 能读取 `metadata.json`
- 能把补充信息覆盖到扫描结果中
- 首页和详情页展示真实合并后的数据

### 阶段 3：接入健康检查

目标：

- 健康检查基于真实数据运行
- 能正确指出缺失字段
- 能从健康检查跳到编辑态

### 阶段 4：保存 sidecar metadata

目标：

- 右侧编辑态保存描述、分类、标签、适用场景
- 成功后立即刷新卡片、详情和健康检查结果
- 刷新页面后数据仍在

---

## 9. 本版结论

这版真正要管理的不是原始文件本身，而是：

`原始技能资产 + 用户对这些资产的整理结果`

sidecar metadata 正好承载这层“整理结果”。

因此，Alpha 的正确方向不是先去改原文件，而是先把下面这条链打通：

`扫描真实技能 -> 展示 -> 健康检查 -> 编辑补充 -> sidecar 持久化`

