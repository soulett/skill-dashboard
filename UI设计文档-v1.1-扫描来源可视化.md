# Skill Dashboard — 扫描来源可视化 UI 设计文档 v1.1

> 日期：2026-04-20  
> 目标：让非技术用户清楚看到系统是否已扫描到 **Codex / Cursor / Claude** 三类本地技能来源

---

## 1. 设计目标

当前问题：用户点击“扫描新技能并更新中文展示”后，不确定系统到底扫描了哪些来源、成功了几个、失败在哪。

本次 UI 目标：

1. 用户一眼看懂“三个平台是否被扫描到”
2. 用户知道“每个平台扫描到多少技能”
3. 用户知道“如果没扫到，下一步该怎么做”

---

## 2. 信息架构（新增）

在首页 Hero 区（按钮下方）新增一个模块：

`扫描来源状态（Source Scan Status）`

模块包含三张来源卡片：

1. Codex
2. Cursor
3. Claude

每张卡片展示：

- 来源名（Codex/Cursor/Claude）
- 当前扫描目录（可折叠显示）
- 扫描状态（已识别 / 未发现 / 路径不可达）
- 识别数量（例如 13 个技能）
- 最近扫描时间（相对时间）

---

## 3. 页面布局草图（线框）

```text
┌──────────────── Hero 区 ────────────────┐
│ [扫描新技能并更新中文展示]               │
│ 只写入 metadata.json，不修改 SKILL.md    │
│                                          │
│ 扫描来源状态                              │
│ ┌──── Codex ────┐ ┌──── Cursor ───┐ ┌──── Claude ───┐
│ │ 已识别 13     │ │ 未发现 0       │ │ 路径不可达     │
│ │ ~/.codex/...  │ │ ~/.cursor/...  │ │ ~/.claude/... │
│ │ 最近扫描 1分钟前│ │ 最近扫描 1分钟前│ │ 最近扫描 1分钟前│
│ └───────────────┘ └───────────────┘ └───────────────┘
│                                          │
│ [查看未识别来源指引]（可选次按钮）        │
└──────────────────────────────────────────┘
```

---

## 4. 状态定义

每个平台卡片只允许以下三态，避免歧义：

1. `已识别`
- 条件：扫描目录存在，且识别技能数 > 0
- 视觉：绿色状态点 + 数量高亮

2. `未发现`
- 条件：扫描目录存在，但识别技能数 = 0
- 视觉：中性灰/琥珀色提示
- 文案：`目录存在，但暂未发现技能文件`

3. `路径不可达`
- 条件：目录不存在或无权限
- 视觉：红色/警示色
- 文案：`未找到该来源目录，可在本机创建后重试`

---

## 5. 交互流程

### 5.1 点击“扫描新技能并更新中文展示”

前端行为：

1. 按钮进入 loading（文案：`正在扫描并更新中文展示...`）
2. 请求扫描接口
3. 刷新技能列表 + 刷新来源状态模块
4. 显示结果 toast：`已更新 X 条，跳过 Y 条`

### 5.2 点击来源卡片

建议交互：

1. 展开目录路径详情（支持复制）
2. 若状态为“未发现/路径不可达”，展示“如何处理”短指引

---

## 6. 文案建议（用户视角）

模块标题：

- `扫描来源状态`

副文案：

- `我们已尝试从 Codex / Cursor / Claude 的常见目录读取本地技能。`

状态文案：

- 已识别：`已识别`
- 未发现：`目录存在，暂未发现技能`
- 路径不可达：`未找到目录或无访问权限`

操作文案：

- 主按钮：`扫描新技能并更新中文展示`
- 次按钮：`查看未识别来源指引`

---

## 7. 数据契约（页面所需）

新增一个来源状态接口（或并入现有 stats）：

```ts
type SourceScanStatus = {
  source: 'codex' | 'cursor' | 'claude';
  label: string;               // "Codex"
  paths: string[];             // 实际尝试扫描的路径
  status: 'detected' | 'empty' | 'unreachable';
  skillCount: number;          // 识别到的技能数
  lastScannedAt: string;       // ISO 时间
  message?: string;            // 补充提示
}
```

页面读取结构：

```ts
{
  sources: SourceScanStatus[];
  totalDetectedSkills: number;
  scannedAt: string;
}
```

---

## 8. MVP 范围（这一版先做什么）

本版建议只做以下最小可用：

1. 首页展示三张来源状态卡（Codex/Cursor/Claude）
2. 扫描后实时刷新状态卡
3. 卡片内显示状态 + 数量 + 路径 + 最近扫描时间
4. 未识别时给出简短下一步指引

暂不做：

1. 手动路径选择器
2. 多平台扩展（Copilot/Windsurf 等）
3. 历史扫描趋势图

---

## 9. 验收标准

满足以下条件即通过：

1. 用户无需懂本地文件夹，也能知道系统是否扫描到三类来源
2. 每次点击扫描后，页面可见变化（状态、数量、时间至少一项变化）
3. 当某来源失败时，用户能看到明确原因与下一步指引

---

## 10. 下一步建议

文档确认后进入实现阶段，按顺序：

1. 后端补充来源状态聚合接口
2. 首页新增“扫描来源状态”模块
3. 扫描按钮结果反馈（已更新/跳过）
4. 异常文案与空状态打磨

