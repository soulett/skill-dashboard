# Skill Dashboard — 产品需求文档 v0.1

> **版本**: v0.1 · 产品定义稿
> **日期**: 2026-04-13
> **状态**: 待确认

---

## 1. 产品定位

**这是什么：**
Skill Dashboard 是一个本地 AI 能力资产的可视化管理工具，它把你电脑上散落的 skills、prompts、SOP 和 agent 配置，整合成一个可查、可用、可复用的个人能力地图。

**它解决谁的什么问题：**
解决 vibe coding 用户"装了很多 skill 却不知道自己有什么、该用什么"的认知断层——让已有的 AI 能力从"存在"变成"真正可调用"。

---

## 2. 目标用户

**核心用户画像：** 非技术背景的 vibe coding 爱好者

| 维度 | 描述 |
|------|------|
| **当前状态** | 用过 Cursor / Claude / ChatGPT，跟着教程装了一批 skills 和 prompts，工具文件夹里已经有十几到几十个配置 |
| **核心问题** | 不知道自己装了哪些、每个能干什么、什么场景该用哪个 |
| **行为表现** | 遇到任务时要么重复发明轮子，要么靠记忆模糊地找文件，大量 skill 处于"装了但从没用过"的状态 |
| **为什么需要这个产品** | 他们有资产，但缺地图。需要一个工具帮他们"看见"已经拥有的 AI 能力，而不是再给他们加更多能力 |

**用户不是：** 专业工程师、prompt 开发者、或者需要团队协作的用户。

---

## 3. 核心功能模块

### 模块 A — Skill 扫描器

| 项目 | 说明 |
|------|------|
| **作用** | 自动读取本地指定目录，识别 skill 文件，解析元信息（名称、描述、标签、触发词等） |
| **解决问题** | 用户不需要手动整理，系统主动帮他"看清家底" |
| **MVP 必要性** | 没有扫描就没有数据，是整个产品的数据入口，不可或缺 |

### 模块 B — Skill 卡片展示（Dashboard 主视图）

| 项目 | 说明 |
|------|------|
| **作用** | 把每个 skill 以卡片形式展示，包含名称、一句话描述、分类标签、适用场景 |
| **解决问题** | 把散落的文件变成人可以"扫一眼就理解"的能力卡片，解决认知负担 |
| **MVP 必要性** | 这是产品核心体验——"看见"自己的 AI 能力。没有这个，产品没有意义 |

### 模块 C — 搜索与筛选

| 项目 | 说明 |
|------|------|
| **作用** | 支持按关键词搜索、按分类/标签筛选，快速定位目标 skill |
| **解决问题** | 当 skill 数量超过 10 个，用户就开始找不到了。搜索是从"看见"到"用得上"的桥梁 |
| **MVP 必要性** | 没有搜索，dashboard 只是个好看的列表，不能解决"找不到"的问题 |

### 模块 D — Skill 详情页

| 项目 | 说明 |
|------|------|
| **作用** | 点击卡片后展示完整 skill 信息：完整描述、触发方式、适用场景、原始内容预览 |
| **解决问题** | 用户知道有这个 skill 后，还需要知道"怎么用"，详情页回答这个问题 |
| **MVP 必要性** | 如果只有卡片没有详情，用户仍然无法真正调用 skill，形成不了使用闭环 |

---

## 4. 页面布局草图

### 4.1 主页面 — Dashboard

```
┌──────────────────────────────────────────────────────────────┐
│  [Logo] Skill Dashboard          [搜索框]        [设置图标]   │
├──────────────┬───────────────────────────────────────────────┤
│              │  筛选标签栏：[全部] [写作] [代码] [分析] [...]  │
│  侧边栏      ├───────────────────────────────────────────────┤
│              │                                               │
│  ▼ 所有技能  │  ┌──────────┐ ┌──────────┐ ┌──────────┐     │
│    (23)      │  │ Skill 卡片│ │ Skill 卡片│ │ Skill 卡片│     │
│              │  │ [图标]   │ │ [图标]   │ │ [图标]   │     │
│  ▼ 按分类    │  │ 标题     │ │ 标题     │ │ 标题     │     │
│    写作 (5)  │  │ 一句话描述│ │ 一句话描述│ │ 一句话描述│     │
│    代码 (8)  │  │ [标签]   │ │ [标签]   │ │ [标签]   │     │
│    分析 (6)  │  └──────────┘ └──────────┘ └──────────┘     │
│    其他 (4)  │                                               │
│              │  ┌──────────┐ ┌──────────┐ ┌──────────┐     │
│  ▼ 最近使用  │  │ Skill 卡片│ │ Skill 卡片│ │  ...     │     │
│              │  └──────────┘ └──────────┘ └──────────┘     │
│              │                                               │
└──────────────┴───────────────────────────────────────────────┘
```

### 4.2 Skill 详情页（右侧抽屉或独立页）

```
┌──────────────────────────────────────────────────────────────┐
│  ← 返回                                              [复制]  │
├──────────────┬───────────────────────────────────────────────┤
│              │                                               │
│  [Skill图标] │  # Skill 名称                                 │
│              │  标签：[写作] [总结]                           │
│  元信息面板  │                                               │
│  ─────────   ├───────────────────────────────────────────────┤
│  分类：写作  │  ## 这个 Skill 能做什么                        │
│  来源：本地  │  一段清晰的用途描述...                          │
│  更新：3天前 │                                               │
│              ├───────────────────────────────────────────────┤
│              │  ## 什么时候用它                               │
│              │  • 场景 1：...                                 │
│              │  • 场景 2：...                                 │
│              │                                               │
│              ├───────────────────────────────────────────────┤
│              │  ## Skill 原始内容                             │
│              │  ┌─────────────────────────────────────────┐ │
│              │  │ [代码/Markdown 预览区]                   │ │
│              │  └─────────────────────────────────────────┘ │
└──────────────┴───────────────────────────────────────────────┘
```

---

## 5. 产品边界

### MVP 必做 ✅

- 扫描本地指定目录，解析 skill 文件
- 卡片式 dashboard 展示
- 关键词搜索 + 标签筛选
- Skill 详情查看（含原始内容预览）

### 第一版不做 ❌

| 不做的事 | 原因 |
|----------|------|
| 编辑 / 修改 skill 内容 | 不是编辑器，避免变成文件管理器 |
| 执行 / 调用 skill（接入 LLM） | 执行层复杂度极高，MVP 聚焦"看清"而非"运行" |
| Workflow 组合 / 串联 | 是后续能力，MVP 先解决"知道有什么"的问题 |
| 云同步 / 多设备 | 先本地，降低复杂度 |
| 社区分享 / 导出 | 面试 demo 不需要，且会分散产品焦点 |
| 使用统计 / 推荐系统 | 数据积累需要时间，MVP 不具备条件 |

---

## 6. 差异化亮点

**和普通 Prompt Manager 的区别：**
Prompt Manager 是"存储已有内容"的工具，用户还是要自己记、自己找、自己懂。Skill Dashboard 的核心动作是**自动扫描 + 解读**——它主动帮你把散落的文件"翻译"成可理解的能力卡片，不需要用户手动录入。

**和文件浏览器的区别：**
文件浏览器只给你看文件名和路径，认知负担在你身上。Skill Dashboard 提取语义信息——你看到的不是 `write-email-skill.md`，而是"这个 skill 帮你写冷邮件，适合陌生人触达场景"。

**和 Workflow Builder 的区别：**
Workflow Builder 假设你已经知道自己有什么，然后帮你连接。Skill Dashboard 解决的是更前置的问题——**先让你知道自己有什么**，这是 workflow 的前提，而不是竞争关系。

**核心切口：**
大多数 AI 工具帮你获取新能力。这个产品帮你盘活已有能力。用户不缺 skill，缺的是对自己 AI 资产的掌控感。

---

## 当前假设（需后续确认）

| # | 假设内容 | 影响范围 |
|---|----------|----------|
| 1 | Skill 文件以 `.md` 或 `.json` 格式存储，包含结构化元信息（如 `skill_id`, `description`, `tags`） | 影响扫描器解析逻辑 |
| 2 | 目标用户使用的是 Cursor 的 Codex Skill 体系（即本地 `.codex/skills/` 目录结构） | 影响扫描路径默认值 |
| 3 | MVP 是纯前端 + 本地文件读取，不需要后端服务 | 影响技术方案选型 |
| 4 | "面试作品集"场景意味着产品需要能独立跑起来 demo，而不是真实部署 | 影响完成度标准 |
| 5 | 用户自己就是创作者和使用者，不涉及权限/多用户 | 简化整个系统设计 |
