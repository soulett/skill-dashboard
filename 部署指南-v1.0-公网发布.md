# Skill Dashboard 公网发布指南（Vercel + Render）

## 1. 目标

让前端页面可以公网访问，并把后端 API 独立部署，支持后续持续更新。

---

## 2. 架构

1. 前端（React + Vite）部署到 Vercel  
2. 后端（Node + Express）部署到 Render  
3. 前端通过 `VITE_API_BASE_URL` 调用 Render API

---

## 3. 后端部署（Render）

1. 在 Render 新建 `Web Service`，连接仓库 `soulett/skill-dashboard`
2. Render 会自动识别根目录的 `render.yaml`
3. 在 Render 环境变量里补充：

- `SILICONFLOW_API_KEY`：你的 API Key
- `CORS_ORIGIN`：你的前端域名（例如 `https://skill-dashboard.vercel.app`）
- `AI_MODEL`：可选，默认 `ZhipuAI/GLM-4.5`

4. 部署成功后，记录 API 地址，例如：

`https://skill-dashboard-api.onrender.com`

---

## 4. 前端部署（Vercel）

1. 在 Vercel 导入同一仓库
2. Build 命令：`npm run build`（已在 `vercel.json` 配置）
3. Output 目录：`dist`（已配置）
4. 在 Vercel 环境变量中设置：

- `VITE_API_BASE_URL=https://skill-dashboard-api.onrender.com`

5. 重新部署

---

## 5. 验证清单

1. 打开前端域名，首页可访问
2. 技能列表可加载（非空 / 无 API 报错）
3. 点击“扫描新技能并更新中文展示”可正常返回
4. Render 日志中 `/api/health` 返回 200

---

## 6. 说明

1. 后端已支持 `PORT/HOST/CORS` 环境变量（适配云平台）
2. 前端已支持 `VITE_API_BASE_URL`（适配跨域 API）
3. 本地开发仍可通过 Vite 代理访问本地 API，不影响当前开发体验
