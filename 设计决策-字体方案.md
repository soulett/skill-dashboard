# 设计决策留痕：字体方案

> 文档版本：v1.0  
> 创建日期：2026-04-21  
> 适用项目：Skill Dashboard

---

## 1. 决策结论

当前项目字体方案统一为：

1. 正文字体：`Noto Sans SC`
2. 标题字体：`Noto Serif SC`
3. 等宽字体：`JetBrains Mono`

同时调整：

1. 首页主标题去除斜体（`italic`）
2. 中文主标题优先可读性，不再使用英文字体风格化斜体表达

---

## 2. 决策背景

用户反馈首页主标题“斜体观感不佳”，尤其在中文场景下，斜体字形会造成：

1. 笔画识别负担上升
2. 视觉重心不稳定
3. 标题“装饰感”大于“信息感”

项目定位为“能力工作台”，应优先保证：

1. 信息清晰可读
2. 风格稳定统一
3. 长期可维护

---

## 3. 选择该方案的原因

### 3.1 `Noto Sans SC`（正文）

1. 中文阅读稳定，字面干净，适合高信息密度界面
2. 在 Windows 环境渲染一致性较好
3. 与深色控制台风格兼容，不会显得过分花哨

### 3.2 `Noto Serif SC`（标题）

1. 保留“编辑感”与品牌感
2. 中文标题识别度高于西文字体替代方案
3. 去掉斜体后仍有层次，不会失去主视觉存在感

### 3.3 `JetBrains Mono`（等宽）

1. 适合路径、代码、时间、计数等技术信息
2. 与现有 UI 语义匹配，迁移成本低

---

## 4. 实施范围

本次改动已落地到：

1. [src/index.css](D:/AI-Coding/skill dashboard/src/index.css)
2. [src/App.tsx](D:/AI-Coding/skill dashboard/src/App.tsx)

具体包括：

1. Google Fonts 引用替换为 `Noto Sans SC`、`Noto Serif SC`、`JetBrains Mono`
2. 主题变量 `--font-sans`、`--font-display` 更新
3. 首页 Hero 主标题移除 `italic` class

---

## 5. 风格规范（后续执行）

后续新增页面遵循：

1. 正文默认使用 `font-sans`（Noto Sans SC）
2. 页面主标题使用 `font-display`（Noto Serif SC），默认不加斜体
3. 技术文本（路径、代码片段、时间戳）使用 `font-mono`
4. 非明确需求下，不再新增其他中文主字体，避免视觉碎片化

---

## 6. 回滚说明

若后续需要回滚到旧方案，可恢复：

1. `src/index.css` 中字体 import 与 `@theme` 字体变量
2. `src/App.tsx` 首页主标题 `italic` 类名

建议回滚前先做两项验证：

1. 中文标题可读性对比截图
2. 首屏视觉风格一致性评审

---

## 7. 变更记录

### 2026-04-21

1. 新建字体设计决策留痕文档
2. 记录字体替换方案与原因
3. 记录首页主标题去斜体决策

