# V1 扫描白名单与统一数据模型

## 目标定义

产品定义不要写成“扫描所有工具的 skill”，而要写成：

`扫描主流 AI 编程工具在本地落盘的可复用 AI 资产`

这里的“资产”包括：

- `skill`
- `rule`
- `memory`
- `command`
- `steering`
- `instruction`
- `prompt`

这意味着产品内部不应直接绑定某一个工具的命名，而应统一抽象为可管理的 `AIAsset`。

## V1 扫描白名单

V1 只扫描官方文档明确、路径相对稳定、用户价值较高的本地目录或文件。

### 1. Codex

- `~/.codex/skills/**`

识别重点：

- `~/.codex/skills/.system/**` -> `sourceType=system`
- `~/.codex/skills/*` 其他目录 -> `sourceType=user-space`

### 2. VS Code / GitHub Copilot

- 工作区：`.github/skills/**`
- 用户级：`~/.copilot/skills/**`

扩展资产：

- `.github/copilot-instructions.md`
- `.github/instructions/**/*.instructions.md`
- `.github/prompts/*.prompt.md`

### 3. Cursor

- `.cursor/rules/**`
- `.cursorrules`
- `AGENTS.md`

### 4. Windsurf

- `.windsurf/rules/**`
- `~/.codeium/windsurf/memories/global_rules.md`
- `AGENTS.md`

### 5. Kiro

- `.kiro/steering/**`
- `~/.kiro/steering/**`
- `AGENTS.md`

### 6. Claude Code

- `./CLAUDE.md`
- `~/.claude/CLAUDE.md`
- `.claude/commands/**`
- `~/.claude/commands/**`

### 7. CodeBuddy

- `.codebuddy/skills/**`
- `~/.codebuddy/skills/**`

## V1 暂不纳入

不建议在 V1 纳入以下情况：

- 路径不稳定或缺少权威文档的产品
- 只能在 UI 设置中配置、但没有明确本地文件位置的规则系统
- 需要靠猜测才能确定来源的目录

因此，`Workbuddy` 暂不进入 V1 扫描白名单。

## 统一数据模型

不要让前端直接理解“这是 skill、这是 rule、这是 memory”。  
统一抽象成一个类型：`AIAsset`

```ts
type AIAsset = {
  id: string
  name: string
  assetType: 'skill' | 'rule' | 'memory' | 'command' | 'instruction' | 'prompt' | 'steering'
  platform: 'codex' | 'copilot' | 'cursor' | 'windsurf' | 'kiro' | 'claude-code' | 'codebuddy' | 'unknown'

  path: string
  scope: 'user' | 'workspace' | 'system'
  sourceType: 'system' | 'user-space' | 'authored' | 'installed' | 'unknown'

  format: 'directory' | 'markdown' | 'json' | 'mixed'
  entryFile?: string

  title?: string
  summary?: string
  tags?: string[]

  provenance?: {
    detectedBy: 'path' | 'metadata' | 'user-confirmed' | 'heuristic'
    confidence: 'high' | 'medium' | 'low'
    authorType?: 'self' | 'official' | 'community' | 'unknown'
    installedFrom?: string
    createdByDashboard?: boolean
  }
}
```

## 三个关键字段

### 1. `platform`

回答：它属于哪个工具生态。

### 2. `assetType`

回答：它本质上是什么资产。

例如：

- Codex -> `skill`
- Cursor -> `rule`
- Windsurf `global_rules` -> `memory` 或 `rule`
- Claude commands -> `command`

### 3. `sourceType`

回答：来源结论到了哪一步。

- `system`
- `user-space`
- `authored`
- `installed`
- `unknown`

注意：

`sourceType` 是产品层的来源判断，不等于平台原始类型。

例如一个 Codex 目录：

- `assetType=skill`
- `sourceType` 可能只是 `user-space`

这并不等于它一定是 `authored`。

## 设计收益

这样整理之后，产品不再依赖某个工具是否也使用“skill”这个名字，而是统一管理不同平台产生的各类 AI 资产。

一句话概括：

`不同工具产生不同名字的资产，但 dashboard 统一把它们当成可管理的 AI 资产来处理。`

## 来源判定规则

这一部分解决的问题不是“理论上有哪些来源类型”，而是：

`系统今天到底能自动拿到什么来源信息，哪些拿不到。`

V1 必须接受一个前提：

`来源数据不是都能从历史文件中识别出来，很多只能从现在开始记录。`

### 判定优先级

来源判定建议按以下优先级执行：

1. 平台路径规则
2. 资产目录中的元信息
3. 产品自身写入的 sidecar 记录
4. 启发式推断
5. 用户手动补录

优先级越高，可信度越高。

## 自动判定：高置信度

以下情况可以直接自动判定，且可信度为 `high`。

### 1. 明确的系统目录

例如：

- `~/.codex/skills/.system/**`

可直接判定为：

- `sourceType=system`
- `provenance.detectedBy=path`
- `provenance.confidence=high`

### 2. 明确的用户级或工作区级官方目录

例如：

- `~/.codex/skills/*`
- `~/.copilot/skills/**`
- `~/.codebuddy/skills/**`
- `.github/skills/**`
- `.cursor/rules/**`
- `.windsurf/rules/**`
- `.kiro/steering/**`
- `.claude/commands/**`

这类只能高置信度判定：

- 它属于某个平台
- 它位于用户空间或工作区空间

但不能直接判定：

- 是用户自己写的
- 还是后来安装的

因此更合适的写法是：

- `scope=user` 或 `scope=workspace`
- `sourceType=user-space`
- `provenance.detectedBy=path`
- `provenance.confidence=high`

注意：

这里的 `high` 只代表“路径识别准确”，不代表“已确认 authored/installed”。

## 自动判定：中置信度

以下情况可以做自动判定，但应该明确标为 `medium`。

### 1. 读取到明确元信息

如果资产目录或入口文件中存在明确字段，例如：

- frontmatter
- `manifest.json`
- `.skill-dashboard.json`
- 其他产品约定的来源字段

可提升为：

- `sourceType=authored` 或 `installed`
- `provenance.detectedBy=metadata`
- `provenance.confidence=medium` 或 `high`

如果该元信息是 dashboard 自己写入的，可以直接给 `high`。

### 2. dashboard 自己创建或安装的资产

如果资产由 dashboard 创建或安装，并且同时写入 sidecar 文件，例如：

```json
{
  "sourceType": "authored",
  "createdByDashboard": true,
  "createdAt": "2026-04-14T10:00:00Z"
}
```

或：

```json
{
  "sourceType": "installed",
  "installedFrom": "official-marketplace",
  "installedAt": "2026-04-14T10:00:00Z"
}
```

则可直接判定：

- `provenance.detectedBy=metadata`
- `provenance.confidence=high`

## 启发式判定：低置信度

以下情况只能做推测，不能作为强事实展示。

例如：

- 目录结构很完整，包含 `LICENSE`、`assets`、`references`、`examples`
- 内容明显像模板化分发包
- 只有一个简单 `SKILL.md`，文本高度个性化，像手写资产
- 存在 repo URL、marketplace URL、作者署名

这类最多只能生成：

- `provenance.detectedBy=heuristic`
- `provenance.confidence=low`
- 推荐标签如：
  - `疑似自建`
  - `疑似安装`
  - `来源未知`

V1 不应该把启发式结果直接显示成确定事实。

## 用户补录策略

对历史资产，很多来源信息无法自动恢复，因此必须支持用户补录。

### 用户可补录的最小选项

建议用户只需要选择：

1. 我自己写的
2. 我安装的
3. 来源不确定

不要在 V1 让用户填太多字段，否则补录成本太高。

### 补录后的写回方式

一旦用户完成补录，dashboard 应把结果写入资产目录下的 sidecar 文件，例如：

`.skill-dashboard.json`

```json
{
  "sourceType": "authored",
  "authorType": "self",
  "userConfirmed": true,
  "updatedAt": "2026-04-14T10:00:00Z"
}
```

之后系统再次扫描时，优先读取这份记录，而不是重新猜测。

## V1 可承诺的能力边界

V1 可以承诺：

- 准确识别一部分平台资产所在目录
- 准确区分 `system` 与 `non-system`
- 统一展示不同工具的 AI 资产
- 对部分资产显示来源置信度
- 支持用户补录来源，并持久化记录

V1 不应承诺：

- 自动识别所有主流 AI 工具的全部资产
- 自动准确识别所有历史资产是否为用户自建
- 仅靠扫描结果就恢复完整来源链路

## 推荐的产品表达

面向用户时，产品不应说：

- “我们能自动识别你所有 skill 的真实来源”

更合适的表达是：

- “我们自动扫描主流 AI 工具落盘的本地 AI 资产”
- “我们优先识别系统资产、用户空间资产和已标记来源”
- “对于无法确定来源的资产，你可以补充标记，系统会持续记住”

## 一句话结论

`来源信息的核心不是“把历史全部识别出来”，而是“先自动识别能确定的部分，再把未来新增和用户补录的部分稳定记录下来”。`

## 页面结构与信息架构

在当前产品定义下，`skill dashboard` 不应只是一个技能列表页，也不应一上来就把用户推入治理动作。

更合适的结构是：

- 首页负责总览
- `Analysis` 负责理解问题
- `Organize` 负责处理问题

一句话概括：

`首页看全貌，分析页看结构，整理页做动作。`

## 首页：Overview

首页的职责不是做深度分析，而是帮助用户在最短时间内建立对当前 AI 资产的全局认知。

首页应该优先回答四个问题：

1. 我现在一共有多少 AI 资产
2. 它们主要分布在哪些平台和类型
3. 哪些资产最值得我关注
4. 目前整体管理状态是否健康

### 首页建议包含的模块

#### 1. 顶部总览卡

建议展示：

- AI 资产总数
- 平台数量
- 来源已确认数量
- 来源未知数量

这个模块的作用是让用户先形成“规模感”。

#### 2. 平台分布

建议展示：

- Codex
- Copilot
- Cursor
- Windsurf
- Kiro
- Claude Code
- CodeBuddy

这个模块回答的是：

`我的资产主要沉淀在哪些工具生态里。`

#### 3. 资产类型分布

建议展示：

- Skill
- Rule
- Memory
- Command
- Instruction
- Prompt
- Steering

这个模块回答的是：

`我手上的 AI 资产，结构上更偏向什么。`

#### 4. 最近关注资产

建议展示少量重点内容，例如：

- 最近新增
- 最近修改
- 最近补录来源
- 来源不明但高频出现的资产

注意：首页不适合展示过长列表，只适合给出少量“值得点进去看”的入口。

#### 5. 管理状态摘要

这一块不是完整的整理中心，而是一个摘要。

例如：

- `18 个资产来源未知`
- `6 个资产缺少描述`
- `4 个资产疑似重复`
- `3 个资产已由你确认来源`

这里的作用是告诉用户：

`你的资产管理目前处于什么状态。`

但不要在首页塞入完整治理工作流。

## Analysis：分析页

`Analysis` 的职责是帮助用户理解资产结构与问题，而不是直接执行批量整理。

它更像一个诊断台。

### Analysis 应回答的问题

1. 我的资产来自哪些平台
2. 哪些来源是确定的，哪些是不确定的
3. 哪些资产类型占比最高
4. 哪些目录或资产最混乱
5. 哪些资产可能重复、过期或价值较低

### Analysis 建议包含的视图

#### 1. 来源分析

展示维度：

- `system`
- `user-space`
- `installed`
- `authored`
- `unknown`

同时展示各来源的置信度情况。

#### 2. 平台交叉分析

例如：

- 哪个平台的资产最多
- 哪个平台来源不明资产最多
- 哪个平台的资产类型最集中

#### 3. 资产健康度分析

建议分析：

- 缺少摘要
- 缺少标签
- 缺少 provenance
- 疑似重复
- 结构不完整

#### 4. 变化趋势

如果后续有时间轴数据，可以加入：

- 最近新增资产趋势
- 最近补录来源趋势
- 最近清理/归档趋势

V1 即使没有时间趋势，也可以先预留模块位置。

## Organize：整理页

`Organize` 的职责是把分析发现的问题转成可执行的整理动作。

这里不强调“理解”，而强调“处理”。

### Organize 应承载的动作

1. 标记来源
2. 补充摘要
3. 添加标签
4. 归类
5. 标记重复
6. 归档或忽略

### Organize 建议的任务分组

#### 1. 待确认来源

展示：

- 来源未知
- 来源低置信度
- 最近新扫描但未确认的资产

用户动作：

- 标记为我创建
- 标记为我安装
- 标记为来源不确定

#### 2. 待补全信息

展示：

- 没有描述
- 没有标签
- 没有标题
- 元信息不完整

用户动作：

- 补摘要
- 加标签
- 修改名称

#### 3. 待清理资产

展示：

- 疑似重复
- 长期未关注
- 结构异常
- 用户明确不再使用

用户动作：

- 归档
- 忽略
- 合并标记

## 页面之间的关系

三个页面不是平级随机功能，而是一个递进关系：

1. 首页建立全局认知
2. `Analysis` 帮用户看懂问题在哪里
3. `Organize` 帮用户真正处理问题

因此它们之间应该支持相互跳转：

- 首页的“管理状态摘要”跳到 `Analysis` 或 `Organize`
- `Analysis` 的问题项跳到 `Organize`
- `Organize` 完成整理后回流到首页和分析页状态

## 首页不应该做的事

为了避免职责混乱，首页不建议承担以下内容：

- 长列表浏览
- 深度来源分析
- 批量整理操作
- 复杂配置管理

这些内容应该下沉到 `Analysis` 或 `Organize`。

## 第一版页面价值主张

如果要用最简短的话定义三页价值，可以写成：

- `Overview`：让我快速知道我拥有哪些 AI 资产
- `Analysis`：让我看懂这些资产是否清晰、完整、可信
- `Organize`：让我把混乱资产逐步整理成可管理状态

## 对应的产品定位

在这个结构下，`skill dashboard` 的产品定位可以写成：

`一个面向个人 AI 资产的总览、分析与整理工作台。`

## 首页第一屏线框级定义

这一部分解决的问题是：

`用户第一次打开首页时，第一屏到底应该看到什么。`

第一屏的目标不是展示全部内容，而是用最短路径回答：

1. 我现在有多少 AI 资产
2. 它们主要来自哪里
3. 当前最值得关注的问题是什么
4. 我下一步可以点去哪里

因此，首页第一屏应该遵循一个基本原则：

`先建立全局感，再给出少量重点入口，不在首屏塞入深度分析和长列表。`

## 首页第一屏推荐结构

建议采用从上到下四段式结构：

1. 页面标题区
2. 总览指标区
3. 核心分布区
4. 状态摘要区

## 1. 页面标题区

这一块要让用户立刻知道自己进入的是“资产总览”，而不是文件浏览器。

建议包含：

- 页面主标题，例如：`AI Asset Overview`
- 一句副标题，例如：`查看你当前分散在不同 AI 工具中的可复用资产`
- 一个主操作入口，例如：`去整理` 或 `查看分析`

作用：

- 建立产品语境
- 说明页面价值
- 给出明确下一步动作

这一块不需要复杂交互，重点是清楚。

## 2. 总览指标区

这是首屏最重要的部分，应该用 3 到 4 个指标卡建立规模感。

建议卡片如下：

### 卡片 1：资产总数

展示：

- 当前 AI 资产总数

作用：

- 让用户立刻知道资产规模

### 卡片 2：平台数

展示：

- 已发现资产的平台数量

作用：

- 建立“分散在多个工具生态中”的感知

### 卡片 3：已确认来源

展示：

- 来源已确认的资产数量或占比

作用：

- 让用户知道自己的资产管理成熟度

### 卡片 4：待确认来源

展示：

- 来源未知或低置信度的资产数量

作用：

- 直接暴露当前最核心的治理缺口

如果首屏空间有限，可以只保留：

- 资产总数
- 平台数
- 待确认来源

## 3. 核心分布区

第一屏中间区域建议放两块并列或上下排列的核心分布视图。

### 视图 A：按平台分布

建议展示：

- Codex
- Copilot
- Cursor
- Windsurf
- Kiro
- Claude Code
- CodeBuddy

可使用：

- 柱状图
- 分段条
- 卡片式占比条

作用：

- 用户能立刻看出自己的资产主要沉淀在哪些工具里

### 视图 B：按资产类型分布

建议展示：

- Skill
- Rule
- Memory
- Command
- Instruction
- Prompt
- Steering

作用：

- 用户能理解自己的资产结构更偏向哪一类能力载体

注意：

这两块图表都应该是“轻分析”，目的是建立认知，不是做深入诊断。

## 4. 状态摘要区

这一块用来承接“管理台”定位，但只做摘要，不做完整任务流。

建议展示 3 到 4 条摘要信息：

- `18 个资产来源未知`
- `6 个资产缺少描述`
- `4 个资产疑似重复`
- `3 个资产最近被你确认来源`

每条摘要都应该能点击跳转：

- 来源相关 -> `Analysis` 或 `Organize`
- 信息缺失 -> `Organize`
- 重复问题 -> `Analysis`

作用：

- 告诉用户目前的管理状态
- 提供非常明确的下一步入口

## 首页首屏信息优先级

如果按优先级排序，建议是：

1. 资产总量与平台范围
2. 来源确认状态
3. 平台分布与类型分布
4. 需要进一步处理的问题摘要

其中最核心的是前两层，因为它们决定用户是否会觉得：

`这个产品真的帮我看清了我当前的 AI 资产全貌。`

## 首页首屏不建议放的内容

以下内容不建议进入第一屏：

- 完整资产列表
- 大量最近文件
- 复杂筛选器
- 批量整理按钮矩阵
- 过深的分析图表
- 平台设置与路径配置

原因很简单：

这些内容会把首页从“总览”拉回“工具台”，削弱用户的全局感。

## 首页首屏推荐阅读顺序

用户进入页面后的阅读路径应尽量自然：

1. 先看到标题，理解这是 AI 资产总览
2. 再看到总览指标，知道规模和状态
3. 接着看到平台/类型分布，理解结构
4. 最后看到状态摘要，决定下一步去分析还是整理

也就是说，首页第一屏应该服务于这个心智流程：

`认知全貌 -> 理解结构 -> 发现问题 -> 进入动作页`

## 首页首屏的页面语气

这一页的表达不应该像工程目录，也不应该像告警中心。

更合适的语气是：

- 冷静
- 总览型
- 资产管理导向

因此文案上应避免：

- “你有很多错误需要处理”
- “立即修复全部问题”
- “检测到严重异常”

更合适的表达是：

- `来源待确认`
- `信息待补全`
- `可进一步整理`
- `查看分析`

## 首页第一屏一句话定义

可以把首页第一屏定义成：

`一个让用户在 5 秒内看清自己 AI 资产规模、结构和管理状态的总览入口。`
